# 🎯 퀴즈 게임 완성 총설계도 (총지휘 문서)

> 파이썬 입문 미션 "나만의 퀴즈 게임"을 **필수 요구사항 + 보너스 5종 전부** 포함하여
> 완성하기 위한, 처음부터 끝까지 이 문서 하나로 진행하는 설계도입니다.
>
> - 작성 기준일: 2026-07-30
> - 사용 도구: 공용 맥(macOS) + 터미널 + VS Code + 깃허브(GitHub) + 네이토(코디세이 AI)
> - 최종 산출물 3가지: ① 동작하는 퀴즈 게임 ② 클래스로 구조화된 코드 ③ 깃허브 저장소(커밋 17개 이상)

---

## 0장. 이 문서를 쓰는 방법 (먼저 5분만 읽어 주세요)

이 문서는 **"단계"라는 벽돌**을 차례대로 쌓는 방식입니다. 단계는 0단계부터 16단계까지 있고,
각 단계는 항상 같은 순서로 진행됩니다.

1. **들어가기 전 확인** — 지금 컴퓨터 상태가 예상과 같은지 확인 명령으로 점검합니다.
2. **네이토에게 보낼 지시문** — 회색 상자 안의 글을 **그대로 복사**해서 네이토에 붙여넣습니다.
3. **받은 코드 검수** — 네이토가 준 코드에서 꼭 있어야 할 것들을 눈으로 확인합니다.
4. **실행 확인** — 프로그램을 돌려서, 문서에 적힌 "기대 화면"과 같은지 비교합니다.
5. **기록(커밋·푸시)** — 깃(Git)으로 저장하고 깃허브에 올립니다.
6. **시험 대비 질문 3개** — 금요일 시험을 위해, 방금 만든 코드를 네이토에게 되물어 공부합니다.

### 꼭 지킬 3가지 원칙

- **한 번에 하나씩.** 명령은 한 줄에 하나만, 순서대로 입력합니다. 여러 명령을 이어 붙이는 축약 기호는 이 문서에 등장하지 않습니다.
- **확인 없이 다음 단계로 가지 않습니다.** 각 명령 아래에는 ① 하는 일 ② 정상일 때 화면 ③ 어긋났을 때 대처가 함께 적혀 있습니다. ②와 다르면 멈추고 ③을 따릅니다.
- **막히면 화면을 통째로 복사합니다.** 그리고 "지금 ○단계인데 이런 화면이 나왔다"라고 저(클로드)나 네이토에게 붙여넣어 물어봅니다. 화면을 요약하지 말고 그대로 붙여넣는 것이 가장 빠릅니다.

### 명령 표기 규칙 (이 문서 전체 공통)

모든 명령 아래에는 다음 형식의 한 줄 설명이 붙습니다.

> ① 하는 일 ② 정상 화면 ③ 어긋나면 → 대처 (자세한 설명은 "3장 명령 카드"의 번호 참조)

파괴적이거나 되돌리기 어려운 동작(병합, 삭제 확인 등)은 반드시 **단독 단계**로 분리되어 있고,
그 앞에는 항상 상태 확인 명령이 먼저 나옵니다. 이 문서에는 **파일을 지우는 명령이 단 하나도 없습니다.**

---

## 1장. 전체 지도 — 단계와 커밋 한눈에 보기

단계 수가 많아 보이지만, **미션이 요구한 커밋 지점과 1 대 1**로 맞춘 것입니다.
(미션은 "메뉴 완성 후 커밋", "Quiz 클래스 후 커밋"처럼 커밋 시점을 직접 지정하고 있어서,
단계를 더 합치면 오히려 미션의 커밋 요건이 깨집니다.)

| 단계 | 할 일 | 네이토 지시문 | 만들어지는 커밋 (누적 번호) |
|---|---|---|---|
| 0 | 공용 맥 점검 (파이썬·깃·계정) | — | — |
| 1 | 깃허브 저장소 + 로컬 연결 + 첫 커밋·푸시 | — (파일 원문은 이 문서에 있음) | ① `Docs: 프로젝트 초기 설정` |
| 2 | 메뉴 뼈대 (game.py, main.py) | P1 | ② `Feat: QuizGame 뼈대와 메뉴, 번호 입력 검증 구현` |
| 3 | Quiz 클래스 (quiz.py) | P2 | ③ `Feat: Quiz 클래스 구현` |
| 4 | 기본 퀴즈 5개 | P3 | ④ `Feat: 기본 퀴즈 데이터 5개 등록` |
| 5 | **가지(브랜치)에서** 퀴즈 풀기 → 병합 | P4 | ⑤ `Feat: 퀴즈 풀기 기능 구현` ⑥ `Merge: 퀴즈 풀기 브랜치 병합` |
| 6 | 퀴즈 추가 | P5 | ⑦ `Feat: 퀴즈 추가 기능 구현` |
| 7 | 퀴즈 목록 | P6 | ⑧ `Feat: 퀴즈 목록 기능 구현` |
| 8 | 점수 확인 (최고 점수) | P7 | ⑨ `Feat: 점수 확인 기능 구현` |
| 9 | state.json 저장·불러오기 | P8 | ⑩ `Feat: state.json 저장·불러오기 구현` |
| 10 | 보너스: 랜덤 출제 + 문제 수 선택 | P9 | ⑪ `Feat: 랜덤 출제와 문제 수 선택 추가` |
| 11 | 보너스: 힌트 (점수 차감) | P10 | ⑫ `Feat: 힌트 기능 추가` |
| 12 | 보너스: 퀴즈 삭제 | P11 | ⑬ `Feat: 퀴즈 삭제 기능 추가` |
| 13 | 보너스: 점수 기록 히스토리 | P12 | ⑭ `Feat: 점수 기록 히스토리 추가` |
| 14 | 다듬기(상수 분리) + README 완성 | P13 | ⑮ `Refactor: 상수 분리와 주석 정리` ⑯ `Docs: README 상세 작성` |
| 15 | 복제(clone)·당겨오기(pull) 실습 | — | ⑰ `Docs: 복제 저장소에서 실습 한 줄 추가` |
| 16 | 제출물 정리 (스크린샷 등) | — | (선택) ⑱ `Docs: 실행 스크린샷 추가` |

- 커밋 합계 **17개(선택 포함 18개)** → 미션 요구 "10개 이상" 여유 있게 충족.
- 깃 명령 7종 사용 위치: `init`·`add`·`commit`·`push`(1단계), `checkout`(5단계), `pull`·`clone`(15단계).
- 브랜치 생성·병합 기록: 5단계에서 확보.

---

## 2장. 완성 모습 미리 보기

무엇을 만드는지 먼저 눈에 넣어 두면, 각 단계가 어디에 붙는 조각인지 알기 쉽습니다.

### 2-1. 완성된 메뉴와 대표 화면

```
========================================
        🎯 나만의 퀴즈 게임 🎯
========================================
1. 퀴즈 풀기
2. 퀴즈 추가
3. 퀴즈 목록
4. 퀴즈 삭제
5. 점수 확인
6. 종료
========================================
선택: 1
몇 문제를 풀까요? (1~5): 3

📝 퀴즈를 시작합니다! (총 3문제)
----------------------------------------
[문제 1]
파이썬의 창시자는?

1. 리누스 토르발스
2. 귀도 반 로섬
3. 제임스 고슬링
4. 브렌던 아이크

정답 입력 (1~4, 힌트 보기는 0): 0
💡 힌트: 네덜란드 출신입니다.
정답 입력 (1~4, 힌트 보기는 0): 2
✅ 정답입니다!
... (중략) ...
========================================
🏆 결과: 3문제 중 3문제 정답! (95점)
   (힌트 1회 사용, 5점 차감)
🎉 새로운 최고 점수입니다!
========================================
```

- 잘못된 입력(`abc`, `9`, 빈 엔터)은 모두 "⚠️ ..." 안내 후 다시 물어봅니다.
- `Ctrl + C`를 눌러도 저장하고 안전하게 종료됩니다.
- 껐다 켜면 "📂 저장된 데이터를 불러왔습니다. (퀴즈 N개, 최고 점수 M점)"이 뜹니다.

### 2-2. 최종 파일 구조

```
python-quiz-game/
├── main.py            # 시작점. 3줄짜리 아주 짧은 파일 (부록 A에 전문)
├── quiz.py            # Quiz 클래스 = "퀴즈 한 문제"의 데이터와 규칙 (부록 B에 전문)
├── game.py            # QuizGame 클래스 = 게임 전체 흐름·메뉴·저장 (부록 D에 설계 지도)
├── state.json         # 데이터 파일. 프로그램이 자동으로 만들고 관리
├── README.md          # 상세 설명 문서 (부록 C에 전문)
├── .gitignore         # 깃이 무시할 파일 목록
└── docs/
    └── screenshots/   # 제출용 화면 캡처 보관
```

### 2-3. state.json 데이터 설계 (스키마)

| 항목(열쇠) | 자료형 | 뜻 |
|---|---|---|
| `quizzes` | 리스트 | 퀴즈들의 목록. 각 퀴즈는 아래 4개 열쇠를 가진 딕셔너리 |
| `quizzes[].question` | 문자열 | 문제 내용 |
| `quizzes[].choices` | 리스트(문자열 4개) | 선택지 |
| `quizzes[].answer` | 정수(1~4) | 정답 번호 |
| `quizzes[].hint` | 문자열 또는 null | 힌트. 없으면 null |
| `best_score` | 정수 또는 null | 최고 점수. 아직 한 번도 안 풀었으면 null |
| `history` | 리스트 | 게임 기록. `date`(문자열), `total`(정수), `correct`(정수), `score`(정수) |

예시:

```json
{
  "quizzes": [
    {
      "question": "파이썬의 창시자는?",
      "choices": ["리누스 토르발스", "귀도 반 로섬", "제임스 고슬링", "브렌던 아이크"],
      "answer": 2,
      "hint": "네덜란드 출신입니다."
    }
  ],
  "best_score": 95,
  "history": [
    { "date": "2026-07-30 14:20", "total": 3, "correct": 3, "score": 95 }
  ]
}
```

### 2-4. 점수 계산 규칙 (한 문장 정리)

> **점수 = (맞힌 수 ÷ 푼 문제 수 × 100, 반올림) − (힌트 사용 횟수 × 5), 최저 0점.**

이렇게 정한 이유: 문제 수를 골라서 풀 수 있으므로(보너스), 개수가 달라도 비교 가능한
백분율 기준이 필요하고, 힌트 차감은 규칙이 단순해야 초보자가 코드로 옮기기 쉽기 때문입니다.

---

## 3장. 설계 원리 — 왜 이렇게 만들었나 (다른 선택지와의 비교 포함)

응용 문제(조건을 바꾼 변형)에 대응하려면, "왜 이 구조인가"를 알아야 합니다.
아래는 이 설계도의 주요 결정과, **일부러 택하지 않은 다른 길**을 함께 적은 것입니다.

**결정 1. 파일을 3개(main / quiz / game)로 나눈다.**
- 택한 이유: "데이터의 모양(quiz.py)"과 "게임의 흐름(game.py)"과 "시작점(main.py)"을 분리하면,
  시험에서 조건이 바뀌었을 때 **고칠 위치가 한 곳으로 좁혀집니다.** (12장 응용 대비표 참조)
- 반대 근거: 파일 1개가 제출·실행이 더 단순하고, 미션도 파일 개수를 강제하지 않습니다.
  다만 파일 1개는 코드가 길어질수록 역설계(리버싱) 학습이 어려워져, 학습 목적상 3개를 택했습니다.

**결정 2. 처음부터 클래스(QuizGame)로 시작한다.**
- 택한 이유: "함수로 먼저 만들고 나중에 클래스로 리팩터"하는 길도 있지만, 그 길은
  중간에 큰 재작성이 필요해서 **초보자 + AI 생성 조합에서 실수 위험이 큽니다.**
- 반대 근거: 미션 문서의 순서(10번 항목)는 나중에 클래스로 정리하는 흐름도 허용하는 것으로 읽힙니다.
  그래서 "정리(Refactor) 커밋"은 14단계에서 **진짜 정리 작업(상수 분리)**으로 별도 확보했습니다.

**결정 3. 정답은 번호(1~4)로 관리한다.**
- 택한 이유: 미션이 직접 권장하는 방식이고, 입력 검증을 숫자 하나로 통일할 수 있습니다.
- 반대 근거: 정답을 문자열로 받아 비교하는 방식도 있습니다. 이 변형은 시험에 나올 수 있어서
  12장 응용 대비표에 "바꾸는 방법"을 적어 두었습니다.

**결정 4. state.json을 깃에 포함해서 커밋한다.**
- 택한 이유: 채점자가 저장소만 받아도 데이터 유지 기능을 바로 확인할 수 있습니다.
- 반대 근거: 실무에서는 데이터 파일을 .gitignore에 넣어 추적하지 않는 경우가 많습니다.
  이 사실은 README(부록 C)에 학습 각주로 남겨 두었습니다. 시험 서술형 대비 포인트입니다.

**결정 5. 병합은 `--no-ff` 방식으로 한다.**
- 택한 이유: 기본 병합은 상황에 따라 "빨리 감기"가 되어 그래프에 병합 흔적이 안 남을 수 있습니다.
  `--no-ff`는 병합 커밋을 반드시 만들어서, 제출용 `git log --oneline --graph` 화면에
  가지가 갈라졌다 합쳐진 모양이 또렷하게 남습니다.
- 반대 근거: 기본 병합이 더 단순합니다. 다만 이번엔 "병합 기록"이 채점 항목이라 흔적을 택했습니다.

**결정 6. 공용 맥에서는 로그인 정보를 저장하지 않는다.**
- 택한 이유: 공용 컴퓨터에 내 계정 열쇠(토큰)가 남으면 다음 사용자가 내 저장소에 접근할 수 있습니다.
  그래서 이 저장소 안에서만 "매번 물어보기" 설정을 하고, 푸시할 때마다 토큰을 붙여넣습니다.
- 반대 근거: 매번 입력은 번거롭습니다. 개인 컴퓨터라면 저장 설정이 일반적이지만, 공용이므로 안전을 택했습니다.

**결정 7. 기본 퀴즈 5개는 이 문서가 직접 확정해서 제공한다.**
- 택한 이유: 퀴즈 내용은 "사실이 틀리면 안 되는" 부분이라, AI가 즉석에서 지어내게 두지 않고
  검증된 상식만으로 이 문서에 고정했습니다(5단계 P3 지시문 안에 원문 포함). 주제를
  "파이썬·깃 기초"로 정하면 **게임을 플레이하는 것 자체가 금요일 시험 복습**이 됩니다.
- 반대 근거: 영화·음악 등 취향 주제가 더 재미있을 수 있습니다. 주제를 바꾸고 싶으면
  P3 지시문 안의 퀴즈 5개만 본인 주제로 갈아 끼우면 되고, 나머지 설계는 전혀 안 바뀝니다.

---

## 4장. 용어 미니 사전

이 문서와 미션에 나오는 낱말을 한글로 풀었습니다. 처음 보는 낱말이 나오면 여기로 돌아오세요.

| 낱말 | 풀이 |
|---|---|
| 터미널 | 글자로 명령을 입력해 컴퓨터를 조작하는 창. 맥에서는 "터미널" 앱 |
| 경로 | 파일이나 폴더의 주소. 예: `/Users/이름/Desktop` |
| 저장소(repository) | 깃이 관리하는 프로젝트 폴더. 깃허브에 있는 것은 "원격 저장소" |
| 커밋(commit) | 지금까지의 변경 내용을 "기록 한 장"으로 확정해 저장하는 것 |
| 푸시(push) | 내 컴퓨터의 커밋들을 깃허브(원격)로 올려 보내는 것 |
| 풀(pull) | 깃허브(원격)의 새 커밋들을 내 컴퓨터로 당겨 오는 것 |
| 클론(clone) | 깃허브의 저장소를 통째로 복제해 내 컴퓨터에 새로 받는 것 |
| 브랜치(branch) | 작업을 나누는 "가지". 본줄기(main)를 건드리지 않고 실험할 수 있음 |
| 병합(merge) | 가지에서 한 작업을 본줄기로 합치는 것 |
| 체크아웃(checkout) | 다른 가지로 갈아타는 것 (가지를 새로 만들며 갈아탈 수도 있음) |
| 클래스(class) | 데이터(속성)와 동작(메서드)을 하나로 묶는 설계도. 붕어빵 틀에 비유 |
| 객체/인스턴스 | 클래스(틀)로 찍어낸 실제 물건. 붕어빵 하나하나 |
| 속성(attribute) | 객체가 가진 값. 예: 퀴즈의 문제, 선택지 |
| 메서드(method) | 클래스 안에 정의된 함수. 예: 정답 확인하기 |
| JSON | 데이터를 글자로 적는 약속된 형식. 딕셔너리·리스트와 모양이 거의 같음 |
| 인코딩(UTF-8) | 글자를 컴퓨터 숫자로 바꾸는 방식. UTF-8이면 한글이 안 깨짐 |
| 토큰(개인 접근 토큰) | 깃허브에 나임을 증명하는 긴 암호 문자열. 비밀번호 대신 사용 |
| 지시문(프롬프트) | AI에게 보내는 요청 글 |
| 리팩터(refactor) | 동작은 그대로 두고 코드 모양만 깨끗하게 고치는 것 |

---

## 5장. 공용 맥 안전 수칙과 세션 루틴

공용 컴퓨터라서 생기는 위험 3가지와, 그 대비책입니다. **매번 앉을 때/일어날 때의 루틴**으로 만들었습니다.

### 5-1. 위험과 대비

1. **내 계정 정보가 남는 위험** → 이 저장소 안에서만 "이름·이메일"을 설정(전역 설정 금지)하고,
   로그인 정보는 저장하지 않도록 설정합니다(1단계에서 수행). 토큰은 공용 맥의 메모장·바탕화면에
   절대 적어 두지 말고, **본인 휴대폰 메모** 등 개인 기기에 보관합니다.
2. **컴퓨터가 초기화되거나 남이 파일을 건드릴 위험** → **모든 단계 끝에 반드시 푸시**합니다.
   깃허브에 올라간 것만이 진짜 내 것입니다.
3. **내 컴퓨터와 깃허브 내용이 서로 다를 위험** → 매 세션 시작 때 상태 확인 + 당겨오기 루틴을 돕니다.

### 5-2. 세션 시작 루틴 (컴퓨터 앞에 앉을 때마다)

```
pwd
```
> ① 지금 내가 어느 폴더에 있는지 출력 ② 경로 한 줄 ③ 프로젝트 폴더가 아니면 `cd`로 이동 (카드 3)

```
ls
```
> ① 폴더 안 파일 목록 출력 ② main.py, game.py 등 내 파일들 ③ 목록이 비었거나 다르면 5-3 복구 절차로 (카드 2)

```
git status
```
> ① 저장소 상태(가지 이름, 변경 여부) 출력 ② `On branch main` + `nothing to commit, working tree clean` ③ 빨간 파일 목록이 보이면 지난 세션에 커밋을 안 한 것 → 내용 확인 후 커밋부터 (카드 8)

```
git pull
```
> ① 깃허브의 새 커밋을 내 컴퓨터로 당겨옴 ② `Already up to date.` 또는 변경 파일 요약 ③ 오류 문구가 나오면 화면을 복사해 두고 진행 중단, 문의 (카드 18)

### 5-3. 컴퓨터가 초기화되었을 때 복구 절차

당황할 필요 없습니다. 깃허브에 푸시해 둔 것이 전부 살아 있습니다.

1. 15단계에서 배우는 `git clone`으로 저장소를 새로 받습니다(카드 19).
2. 새로 받은 폴더에서 1단계의 "이름·이메일 설정"과 "매번 물어보기 설정"을 다시 합니다.
3. 5-2 루틴대로 상태를 확인하고 이어서 작업합니다.

### 5-4. 세션 종료 루틴 (일어나기 전에)

```
git status
```
> ① 커밋 안 된 변경이 있는지 확인 ② `nothing to commit, working tree clean` ③ 변경이 남아 있으면 커밋·푸시 후 종료 (카드 8)

```
git log --oneline -3
```
> ① 최근 커밋 3개를 한 줄씩 출력 ② 방금 한 커밋이 맨 위에 보임 ③ 안 보이면 커밋이 누락된 것 → 해당 단계의 기록 절차 다시 수행 (카드 20)

마지막으로 깃허브 웹사이트에서 내 저장소를 열어 방금 커밋이 올라와 있는지 눈으로 확인하면 완벽합니다.

---

## 6장. 명령 카드 (카드 1~22)

이 문서에 나오는 모든 명령의 상세 설명서입니다. 본문에서는 "(카드 N)"으로 이곳을 가리킵니다.
공통 원칙: **모르는 오류 화면이 나오면 절대 다음 명령으로 넘어가지 말고, 화면을 복사해서 문의합니다.**

**카드 1. `pwd`**
- ① 하는 일: 지금 내가 서 있는 폴더의 전체 경로를 보여줍니다. ("나 지금 어디지?")
- ② 정상 화면: `/Users/사용자이름/Desktop/python-quiz-game` 같은 경로 한 줄.
- ③ 어긋나면: 기대한 폴더가 아니면 카드 3의 `cd`로 이동합니다. 오류가 날 일은 거의 없습니다.

**카드 2. `ls`**
- ① 하는 일: 지금 폴더 안의 파일·폴더 목록을 보여줍니다.
- ② 정상 화면: 파일 이름들이 나열됨. 아무것도 없으면 빈 줄(빈 폴더라는 뜻, 오류 아님).
- ③ 어긋나면: 있어야 할 파일이 안 보이면, 카드 1로 위치부터 다시 확인합니다.

**카드 3. `cd 폴더이름`**
- ① 하는 일: 해당 폴더 안으로 들어갑니다. `cd ..`는 한 단계 위로 나갑니다.
- ② 정상 화면: 아무 출력 없음(조용히 성공). `pwd`로 확인하면 경로가 바뀌어 있음.
- ③ 어긋나면: `No such file or directory` → 폴더 이름 오타이거나 위치가 다름. `ls`로 목록을 보고 정확한 이름을 다시 입력합니다. 이름에 공백이 있으면 따옴표로 감쌉니다.

**카드 4. `mkdir 폴더이름`**
- ① 하는 일: 새 폴더를 만듭니다.
- ② 정상 화면: 아무 출력 없음. `ls`로 확인하면 새 폴더가 보임.
- ③ 어긋나면: `File exists` → 이미 같은 이름의 폴더가 있음. 그냥 그 폴더를 쓰면 되므로 오류 아님. 단, 그 폴더 안 내용이 무엇인지 `ls`로 꼭 확인합니다.

**카드 5. `cat 파일이름`**
- ① 하는 일: 파일 내용을 화면에 그대로 출력합니다. (state.json 확인 등에 사용)
- ② 정상 화면: 파일 내용 전체.
- ③ 어긋나면: `No such file` → 파일이 아직 안 만들어졌다는 뜻. 첫 실행 전이라면 정상일 수 있습니다.

**카드 6. `git --version`**
- ① 하는 일: 깃이 설치되어 있는지, 버전이 무엇인지 확인합니다.
- ② 정상 화면: `git version 2.XX.X` 형태 한 줄.
- ③ 어긋나면: `command not found` → 깃 미설치. 맥에서는 이 명령을 치면 개발자 도구 설치 창이 뜨는 경우가 많으니 설치를 진행하고, 안 뜨면 과정 운영진에게 문의합니다.

**카드 7. `python3 --version`**
- ① 하는 일: 파이썬 버전을 확인합니다. 미션 조건은 3.10 이상입니다.
- ② 정상 화면: `Python 3.1X.X` (3.10, 3.11, 3.12, 3.13 모두 좋음).
- ③ 어긋나면: 3.10 미만이거나 `command not found` → 진행을 멈추고 운영진 문의 또는 python.org 공식 설치 파일로 설치합니다. 공용 맥이므로 임의 설치 전에 운영진 확인을 권합니다.

**카드 8. `git status`**
- ① 하는 일: 지금 가지 이름, 커밋 안 된 변경 목록 등 저장소의 "현재 상태"를 보여줍니다. 가장 자주 쓰는 안전 확인 명령입니다.
- ② 정상 화면: 작업 직후라면 빨간색 파일 목록(= 아직 커밋 후보에 안 올라간 변경), 커밋 직후라면 `nothing to commit, working tree clean`.
- ③ 어긋나면: `not a git repository` → 프로젝트 폴더 밖에 있는 것. 카드 1, 3으로 위치를 잡습니다.

**카드 9. `git config user.name` / `git config user.email` (확인) 그리고 `git config user.name "이름"` (설정)**
- ① 하는 일: 커밋에 찍힐 작성자 이름·이메일을 확인/설정합니다. **`--global`을 붙이지 않으면 지금 저장소에서만 적용**됩니다. 공용 맥에서는 절대 `--global`을 쓰지 않습니다(다른 사용자에게 영향).
- ② 정상 화면: 확인 시 저장된 값 한 줄(비어 있으면 아무것도 안 나옴), 설정 시 아무 출력 없음.
- ③ 어긋나면: 커밋할 때 `Please tell me who you are` 오류가 나면 이 설정이 빠진 것 → 저장소 폴더 안에서 설정 명령 2개(이름, 이메일)를 실행합니다. 이메일은 본인 깃허브 계정 이메일을 씁니다.

**카드 10. `git init`**
- ① 하는 일: 지금 폴더를 깃 저장소로 만듭니다(숨은 관리 폴더 생성).
- ② 정상 화면: `Initialized empty Git repository in .../python-quiz-game/.git/`
- ③ 어긋나면: `Reinitialized existing ...` → 이미 저장소였다는 뜻. 이상한 상태일 수 있으니 `git status`와 `git log --oneline`으로 내용을 확인하고, 낯선 커밋이 보이면 진행을 멈추고 문의합니다.

**카드 11. `git branch -M main` / `git branch`**
- ① 하는 일: `-M main`은 지금 가지 이름을 main으로 바꿉니다(예전 깃은 기본 이름이 다를 수 있어서 통일). 인자 없이 `git branch`는 가지 목록을 보여주며 현재 가지에 `*` 표시가 붙습니다.
- ② 정상 화면: 이름 변경은 아무 출력 없음. 목록은 `* main` 등.
- ③ 어긋나면: 목록에서 `*`가 기대한 가지에 없으면 카드 16으로 갈아탑니다.

**카드 12. `git remote add origin 주소` / `git remote -v`**
- ① 하는 일: 내 저장소에 "origin"이라는 별명으로 깃허브 주소를 등록합니다. `-v`는 등록된 주소를 확인합니다.
- ② 정상 화면: 등록은 아무 출력 없음. `-v`는 `origin https://github.com/아이디/python-quiz-game.git (fetch)`와 `(push)` 두 줄.
- ③ 어긋나면: `remote origin already exists` → 이미 등록됨. `git remote -v`로 주소를 확인하고, 주소가 틀렸을 때만 `git remote set-url origin 올바른주소`로 고칩니다.

**카드 13. `git add .`**
- ① 하는 일: 지금 폴더의 모든 변경을 "커밋 후보 바구니"에 담습니다.
- ② 정상 화면: 아무 출력 없음. `git status`로 보면 파일들이 초록색으로 바뀜.
- ③ 어긋나면: 오류 문구가 나오면 그대로 복사해 두고 멈춥니다. (드묾)

**카드 14. `git commit -m "메시지"`**
- ① 하는 일: 바구니에 담긴 변경을 메시지와 함께 기록 한 장으로 확정합니다. `-m`을 꼭 붙입니다(안 붙이면 낯선 편집 화면에 갇힐 수 있음).
- ② 정상 화면: `[main 짧은코드] 메시지` + `N files changed ...` 요약.
- ③ 어긋나면: `Please tell me who you are` → 카드 9 수행 후 다시 커밋. `nothing to commit` → 담긴 변경이 없다는 뜻, 카드 13부터 다시.

**카드 15. `git push -u origin main` (첫 푸시) / `git push` (이후)**
- ① 하는 일: 내 커밋들을 깃허브로 올립니다. `-u`는 "다음부터는 `git push`만 쳐도 되게" 연결을 기억시키는 옵션이라 첫 푸시에만 씁니다. 가지를 처음 올릴 때는 `git push -u origin 가지이름`.
- ② 정상 화면: 아이디(Username)와 비밀번호(Password)를 물어봄 → 아이디는 깃허브 아이디, **비밀번호 자리에는 토큰을 붙여넣기** 합니다. **토큰을 붙여넣어도 화면에 아무 글자도 안 보이는 것이 정상**입니다(보안). 엔터를 치면 `... -> main` 같은 업로드 요약이 나옵니다.
- ③ 어긋나면: `403` 또는 `Permission denied`에 **다른 사람 아이디**가 보이면 공용 맥에 남의 로그인 정보가 저장된 상태 → 카드 22를 이 저장소에서 실행한 뒤 다시 푸시합니다. `Support for password authentication was removed` → 비밀번호 대신 토큰을 넣어야 한다는 안내이므로 토큰을 사용합니다. `repository not found` → 주소 오타, 카드 12로 확인.

**카드 16. `git checkout -b 가지이름` (새로 만들며 갈아타기) / `git checkout main` (갈아타기)**
- ① 하는 일: 작업 가지를 바꿉니다. `-b`는 새 가지를 만들면서 이동합니다.
- ② 정상 화면: `Switched to a new branch '가지이름'` 또는 `Switched to branch 'main'`.
- ③ 어긋나면: `Please commit your changes ...` → 커밋 안 된 변경이 있어 이동을 막은 것. 먼저 커밋(카드 13, 14)하고 다시 시도합니다.

**카드 17. `git merge --no-ff 가지이름 -m "메시지"`**
- ① 하는 일: 가지의 작업을 지금 가지(main)로 합치고, 병합 커밋을 반드시 남깁니다. `-m`으로 편집 화면을 건너뜁니다.
- ② 정상 화면: `Merge made by the 'ort' strategy.` + 변경 파일 요약.
- ③ 어긋나면: `CONFLICT`(충돌)라는 단어가 보이면 **아무것도 더 입력하지 말고** 카드 21로 병합을 안전하게 취소한 뒤, 화면 전체를 복사해 문의합니다. (이 설계도 순서대로면 main을 병행 수정하지 않아 충돌이 나지 않습니다.)

**카드 18. `git pull`**
- ① 하는 일: 깃허브의 새 커밋을 내 컴퓨터로 당겨와 합칩니다.
- ② 정상 화면: 새 것이 없으면 `Already up to date.`, 있으면 `Fast-forward`와 변경 파일 요약.
- ③ 어긋나면: `CONFLICT` → 카드 21 상황과 동일하게 멈추고 문의. `There is no tracking information` → 첫 연결이 안 된 상태이니 `git pull origin main`으로 대상을 지정해 실행합니다.

**카드 19. `git clone 주소 새폴더이름`**
- ① 하는 일: 깃허브 저장소를 통째로 복제해서 "새폴더이름"이라는 폴더로 받습니다. 폴더는 자동 생성됩니다.
- ② 정상 화면: `Cloning into '새폴더이름'...` ~ `done.`
- ③ 어긋나면: `Authentication failed` → 아이디/토큰 다시 입력. `already exists and is not an empty directory` → 같은 이름 폴더가 이미 있음. 다른 이름을 지정합니다(기존 폴더를 지우지 않습니다).

**카드 20. `git log --oneline --graph` / `git log --oneline -3`**
- ① 하는 일: 커밋 역사를 한 줄씩, `--graph`는 가지 모양 그림과 함께 보여줍니다. `-3`은 최근 3개만.
- ② 정상 화면: `* 짧은코드 메시지` 줄들. 병합이 있으면 선이 갈라졌다 합쳐지는 모양.
- ③ 어긋나면: **화면이 멈춘 것처럼 보이면 목록이 길어서 "넘겨보기 모드"에 들어간 것 → 키보드 `q`를 누르면 빠져나옵니다.** (오류 아님. 스페이스로 다음 장, `q`로 종료)

**카드 21. `git merge --abort` (안전 후퇴)**
- ① 하는 일: 진행 중이던 병합을 없던 일로 되돌립니다. 충돌이 났을 때의 안전 버튼입니다.
- ② 정상 화면: 아무 출력 없음. `git status`가 다시 깨끗해짐.
- ③ 어긋나면: 오류가 나오면 화면 복사 후 문의. 다른 명령을 추가로 시도하지 않습니다.

**카드 22. `git config credential.helper ""` (이 저장소만 "매번 물어보기")**
- ① 하는 일: **지금 저장소에서만** 로그인 정보 저장 기능을 끕니다. 푸시할 때마다 아이디와 토큰을 새로 입력하게 되어, 공용 맥에 내 열쇠가 남지 않고 남의 열쇠 때문에 꼬이지도 않습니다.
- ② 정상 화면: 아무 출력 없음.
- ③ 어긋나면: 이 설정 뒤에도 푸시가 아이디를 묻지 않고 곧바로 실패하면 화면을 복사해 문의합니다. (따옴표 두 개 `""`를 그대로 입력해야 합니다.)

---

## 7장. 0단계 — 공용 맥 점검 (커밋 없음, 5~10분)

여기서는 아무것도 만들지 않고 **확인만** 합니다. 아래 명령을 하나씩 실행하고,
②(정상 화면)와 다르면 다음 단계로 넘어가지 않습니다.

```
git --version
```
> ① 깃 설치 확인 ② `git version 2.XX.X` ③ 설치 창이 뜨면 설치, 안 되면 문의 (카드 6)

```
python3 --version
```
> ① 파이썬 3.10 이상인지 확인 ② `Python 3.1X.X` ③ 3.10 미만/미설치면 진행 중단, 문의 (카드 7)

```
git config user.name
```
> ① 이 컴퓨터에 저장된 커밋 작성자 이름 확인 ② 이름 또는 빈 출력 ③ **남의 이름이 보여도 지우지 않습니다.** 1단계에서 "내 저장소 안에서만" 내 이름을 따로 설정할 것이므로 그대로 둡니다 (카드 9)

```
git config user.email
```
> ① 저장된 이메일 확인 ② 이메일 또는 빈 출력 ③ 위와 동일: 손대지 않고 1단계에서 저장소 전용 설정 (카드 9)

**작업 위치 정하기.** 본인 이름이 드러나는 개인 폴더를 하나 정합니다. 이 문서는 예시로
`바탕화면(Desktop)` 아래를 사용합니다. 경로에 **한글·공백이 없는 폴더 이름**을 권합니다(오류 예방).

```
cd ~/Desktop
```
> ① 바탕화면 폴더로 이동 ② 출력 없음 ③ 다른 위치를 쓰고 싶으면 그 경로로 이동, 이후 단계의 경로도 같게 유지 (카드 3)

```
pwd
```
> ① 위치 확인 ② `/Users/사용자이름/Desktop` ③ 다르면 위 `cd`부터 다시 (카드 1)

**준비물 확인(터미널 밖).**
- 깃허브 계정 아이디/비밀번호로 웹 로그인이 되는지 확인합니다.
- VS Code가 열리는지 확인합니다. (응용 프로그램 폴더 → Visual Studio Code)
- 네이토 접속이 되는지 확인합니다.

여기까지 전부 ②대로면 0단계 통과입니다.

---

## 8장. 1단계 — 깃허브 저장소 만들기와 첫 커밋·푸시 (커밋 ①)

### 8-1. 깃허브 웹에서 빈 저장소 만들기

1. 깃허브에 로그인한 뒤, 화면 **오른쪽 위의 `+` 버튼 → `New repository`**를 누릅니다.
   (버튼 문구가 조금 달라도 "New repository"를 찾으면 됩니다.)
2. Repository name(저장소 이름)에 **`python-quiz-game`** 을 입력합니다.
3. 공개 여부는 **Public(공개)** 을 선택합니다. (제출용 주소 공유가 쉬움)
4. **주의: "Add a README file" 등 초기화 항목은 전부 체크하지 않습니다.**
   우리는 내 컴퓨터에서 `git init`으로 시작해 올릴 것이라, 깃허브 쪽에 파일이 미리 있으면
   첫 푸시 때 충돌이 생길 수 있습니다.
5. `Create repository` 버튼을 누릅니다.
6. 생성 후 나오는 안내 화면에서 **`https://github.com/내아이디/python-quiz-game.git`** 주소를
   복사해 둡니다. (화면의 HTTPS 주소)

### 8-2. 개인 접근 토큰 만들기 (푸시할 때 비밀번호 대신 사용)

깃허브는 2021년부터 명령어 창에서 비밀번호 대신 토큰을 요구합니다. 한 번만 만들어 두면 됩니다.

1. 깃허브 오른쪽 위 프로필 사진 → `Settings`(설정) → 왼쪽 맨 아래 `Developer settings`.
2. `Personal access tokens` → `Tokens (classic)` → `Generate new token (classic)`.
3. Note(메모)에 용도를 적습니다. 예: `quiz-game 과제용`.
4. Expiration(만료 기간)은 90일 정도로 정합니다.
5. 권한(scopes) 목록에서 **`repo`** 하나만 체크합니다.
6. 맨 아래 `Generate token` → 생성된 긴 문자열을 **즉시 복사**합니다.
   **이 화면을 벗어나면 다시 볼 수 없습니다.** 본인 휴대폰 메모 등 **개인 기기에만** 보관합니다.
   (공용 맥의 메모장·바탕화면 저장 금지)

- 참고: 깃허브는 더 잘게 권한을 쪼개는 새 방식(fine-grained) 토큰도 제공하고 그 방식을 권장하지만,
  이 과제에는 설정 항목이 적은 classic + `repo` 권한이 초보자에게 실수 여지가 적어 이 방식을 택했습니다.
  (반대 근거와 함께 3장 결정 6·7 참조)

### 8-3. 로컬 폴더 만들기와 저장소 초기화

```
cd ~/Desktop
```
> ① 작업 위치로 이동 ② 출력 없음 ③ 카드 3

```
mkdir python-quiz-game
```
> ① 프로젝트 폴더 생성 ② 출력 없음 ③ `File exists`면 폴더 내용 확인 후 사용 (카드 4)

```
cd python-quiz-game
```
> ① 프로젝트 폴더로 진입 ② 출력 없음 ③ 카드 3

```
pwd
```
> ① 위치 최종 확인 ② `/Users/사용자이름/Desktop/python-quiz-game` ③ 다르면 위부터 다시 (카드 1)

```
git init
```
> ① 이 폴더를 깃 저장소로 만듦 ② `Initialized empty Git repository ...` ③ `Reinitialized`가 나오면 카드 10의 대처 확인 (카드 10)

```
git branch -M main
```
> ① 기본 가지 이름을 main으로 통일 ② 출력 없음 ③ 카드 11

### 8-4. 공용 맥 보호 설정 (이 저장소 안에서만 적용)

```
git config user.name "본인이름"
```
> ① 이 저장소의 커밋 작성자 이름 설정(전역 아님) ② 출력 없음 ③ 따옴표 포함해 정확히 입력. `--global`은 절대 붙이지 않음 (카드 9)

```
git config user.email "깃허브가입이메일"
```
> ① 이 저장소의 커밋 이메일 설정 ② 출력 없음 ③ 깃허브 계정에 등록된 이메일과 같아야 잔디(기여 기록)가 본인 계정에 찍힘 (카드 9)

```
git config credential.helper ""
```
> ① 이 저장소에서 로그인 정보 저장 기능 끔(매번 물어보기) ② 출력 없음 ③ 카드 22

```
git config user.name
```
> ① 방금 설정 확인 ② 본인 이름 출력 ③ 다르면 설정 명령부터 다시 (카드 9)

### 8-5. 첫 파일 2개 만들기 (VS Code 사용)

VS Code를 열고 `파일 → 폴더 열기`로 방금 만든 `python-quiz-game` 폴더를 엽니다.
"작성자를 신뢰합니까?" 창이 뜨면 신뢰를 선택합니다. 그다음 새 파일 2개를 만들고
아래 내용을 **그대로** 붙여넣고 저장(⌘S)합니다.

**파일 1: `.gitignore`** (점으로 시작하는 이름 그대로)

```
# 파이썬이 실행 중 자동으로 만드는 임시 파일 (내 코드가 아니므로 깃에서 제외)
__pycache__/
*.pyc

# 맥 운영체제가 폴더마다 자동으로 만드는 파일
.DS_Store

# 편집기(VS Code) 개인 설정
.vscode/
```

**파일 2: `README.md`** (지금은 초안, 14단계에서 완성)

```
# 🎯 나만의 퀴즈 게임

파이썬으로 만드는 터미널 퀴즈 게임입니다. (작성 중 — 완성본은 개발 완료 후 갱신)
```

- 참고: `state.json`은 .gitignore에 넣지 않았습니다. 이유와 반대 근거는 3장 결정 4에 있습니다.

### 8-6. 첫 커밋과 첫 푸시

```
git status
```
> ① 새 파일 2개가 인식되는지 확인 ② 빨간색으로 `.gitignore`, `README.md` ③ 안 보이면 저장(⌘S) 여부와 폴더 위치 확인 (카드 8)

```
git add .
```
> ① 변경 전부를 커밋 후보에 담기 ② 출력 없음 ③ 카드 13

```
git commit -m "Docs: 프로젝트 초기 설정 (README 초안, .gitignore)"
```
> ① 커밋 ① 생성 ② `[main ...] Docs: ...` + `2 files changed` ③ `Please tell me who you are`면 8-4부터 다시 (카드 14)

```
git remote add origin https://github.com/내아이디/python-quiz-game.git
```
> ① 깃허브 주소를 origin이라는 별명으로 등록 (**내아이디 부분을 본인 것으로 교체**) ② 출력 없음 ③ `already exists`면 카드 12의 대처 (카드 12)

```
git remote -v
```
> ① 등록된 주소 확인 ② 내 아이디가 들어간 주소 두 줄 ③ 틀렸으면 카드 12로 수정 (카드 12)

다음은 인터넷으로 올리는 동작이므로 **단독으로** 실행합니다.

```
git push -u origin main
```
> ① 커밋 ①을 깃허브로 올림 ② Username에 깃허브 아이디, Password에 **토큰 붙여넣기(화면에 안 보이는 게 정상)** → 업로드 요약 출력 ③ 403/다른 아이디가 보이면 카드 15와 22 확인 (카드 15)

**최종 확인:** 웹 브라우저에서 내 저장소를 새로고침 → `README.md`와 `.gitignore`가 보이면 1단계 완료입니다.

---

## 9장. 네이토 운용 규칙 — 코드 생성 AI를 안전하게 부리는 법

네이토는 입력 글자 수 한도(한글 약 5,000자)가 있지만, 이 문서의 지시문은 **가장 긴 것도 실측 1,611자**로
설계했으므로 전부 한 번에 넉넉히 붙여넣을 수 있습니다. 대화 전체 기억 용량은 넉넉하므로(100만 토큰),
**한 대화방에서 끝까지 이어서** 진행하는 것을 기본으로 합니다.

- 모델 선택 조언: 코드를 만들거나 고치는 단계(P1~P13)는 상위 모델(클로드 오푸스 계열 또는 지피티 상위 모델)을,
  "시험 대비 질문"처럼 설명만 듣는 대화는 빠른 모델(제미나이 플래시 계열)을 쓰면 자원을 아낄 수 있습니다.

### 9-1. 맨 처음 한 번: 규칙 고정 지시문 P0 (그대로 복사해서 네이토에 붙여넣기)

```
지금부터 너는 파이썬을 처음 배우는 나의 짝 프로그래머다. 우리는 터미널 퀴즈 게임을 만든다.
이 대화가 끝날 때까지 아래 규칙을 지켜라.

[프로젝트 정보]
- 파일 3개: main.py(시작점), quiz.py(Quiz 클래스), game.py(QuizGame 클래스)
- 데이터는 프로젝트 루트의 state.json (UTF-8)
- 파이썬 3.10 이상, 외부 라이브러리 금지 (표준 라이브러리 json, random, datetime만 허용)
- input()과 print()로 동작하는 터미널 프로그램

[출력 규칙]
1. 내가 "[N단계]"로 시작하는 지시문을 주면, 요청한 파일의 전체 내용을 처음부터 끝까지
   코드 상자 하나로 출력한다. 일부 생략, "...", "(이전과 동일)" 같은 표기는 금지한다.
2. 모든 코드에 한국어 주석을 풍부하게 단다. 파일 맨 위에는 그 파일의 역할 요약 주석,
   함수와 메서드마다 역할·매개변수·반환값 주석, 초보자가 헷갈리는 문법(self, f-문자열,
   enumerate 등)에는 한 줄 풀이 주석을 단다.
3. 지시문에 없는 기능을 미리 추가하지 않는다. 요청한 범위만 정확히 구현한다.
4. 코드 상자 아래에 "이번에 새로 쓰인 문법 3가지"를 각각 2줄 이내로 설명한다.
5. 앞 단계에서 정한 클래스·메서드·변수 이름을 절대 바꾸지 않는다.
6. 어려운 영어 용어는 한국어로 풀어서 설명한다.

이해했으면 "준비 완료" 네 글자만 답하라.
```

### 9-2. 매 단계 공통 절차 (2~14단계에서 반복)

1. **시작 확인.** 터미널에서 아래 2개를 실행해 위치와 상태를 봅니다.

```
pwd
```
> ① 위치 확인 ② `.../python-quiz-game` ③ 다르면 이동 (카드 1, 3)

```
git status
```
> ① 이전 단계가 깨끗이 커밋됐는지 확인 ② `nothing to commit, working tree clean` ③ 빨간 변경이 남았으면 이전 단계 커밋부터 (카드 8)

2. **지시문 붙여넣기.** 해당 단계의 지시문(P1~P13)을 통째로 복사해 네이토에 보냅니다.
3. **3종 검수.** 받은 코드에서 ⑴ 파일 맨 위 주석의 파일 이름이 맞는지 ⑵ 생략 표시가 없는지
   ⑶ 그 단계의 "검수 포인트"가 모두 들어 있는지 확인합니다. ⑵에 걸리면 아래 재요청 문구를 보냅니다.

```
방금 출력에 생략된 부분이 있다. 규칙 1에 따라 해당 파일 전체를 처음부터 끝까지 다시 출력하라.
```

4. **파일에 반영.** VS Code에서 해당 파일을 열고, 전체 선택(⌘A) 후 붙여넣기(⌘V)로
   **파일 내용을 통째로 교체**하고 저장(⌘S)합니다. 새 파일이면 새로 만들어 붙여넣습니다.
5. **실행 확인.** 터미널에서 실행해 그 단계의 "기대 화면"과 비교합니다. 다르면 아래
   **오류 수리 지시문**에 터미널 화면을 그대로 붙여 네이토에 보냅니다.

```
[오류 수리] 아래는 python3 main.py 실행 결과 화면이다. 원인을 초보자 눈높이로 설명하고,
문제가 있는 파일 전체를 고쳐서 다시 출력하라.
(여기에 터미널 화면 전체를 붙여넣기)
```

6. **기록.** 각 단계 끝의 커밋·푸시 명령 3개를 순서대로 실행합니다.
7. **되새김.** 각 단계 끝의 "시험 대비 질문 3개"를 네이토에게 물어보고 답을 읽습니다.

### 9-3. 대화방이 끊겼을 때 복구법

새 대화방을 열고 → P0을 다시 보내고 → 아래 문구를 보낸 뒤,

```
지금까지 만든 파일을 붙여넣겠다. 글자 수 제한 때문에 여러 번에 나눠 보낼 테니,
내가 "끝"이라고 보낼 때까지 아무 답도 하지 말고 기다려라.
```

VS Code에서 quiz.py, game.py 내용을 **5,000자가 넘지 않게 몇 덩어리로 나눠** 차례로 붙여넣고,
마지막에 아래를 보냅니다.

```
끝. 위 파일들이 현재 상태다. 이제 이어서 [N단계] 지시문을 수행하라.
```

---

## 10장. 2~13단계 — 기능을 하나씩 쌓기

각 단계는 9-2 공통 절차를 따르고, 아래에는 단계별 고유 내용(지시문, 검수 포인트, 기대 화면,
커밋, 질문)만 적습니다.

---

### 2단계. 메뉴 뼈대 — game.py, main.py (커밋 ②)

**네이토 지시문 P1 (그대로 복사)**

```
[2단계] game.py와 main.py 두 파일을 새로 만든다. 각 파일의 전체 내용을 별도의 코드 상자로 출력하라.

(1) game.py — QuizGame 클래스
- __init__(self): self.quizzes = [] 로 퀴즈 목록 자리를 만든다. (데이터는 다음 단계에서 채운다는 주석)
- show_menu(self): 아래 메뉴를 이 모양 그대로 출력한다.
========================================
        🎯 나만의 퀴즈 게임 🎯
========================================
1. 퀴즈 풀기
2. 퀴즈 추가
3. 퀴즈 목록
4. 퀴즈 삭제
5. 점수 확인
6. 종료
========================================
- get_number_input(self, prompt, min_num, max_num):
  while True 반복 안에서 input(prompt)로 받고 .strip()으로 앞뒤 공백을 제거한다.
  빈 문자열이면 "⚠️ 아무것도 입력되지 않았습니다. 다시 입력하세요." 출력 후 반복.
  int 변환 실패(try/except ValueError)면 "⚠️ 숫자를 입력하세요." 출력 후 반복.
  min_num~max_num 범위 밖이면 f"⚠️ {min_num}~{max_num} 사이의 숫자를 입력하세요." 출력 후 반복.
  통과하면 정수를 반환한다. KeyboardInterrupt와 EOFError는 여기서 잡지 않는다(위에서 처리).
- run(self): while True 반복 전체를 try로 감싼다.
  show_menu() → choice = self.get_number_input("선택: ", 1, 6)
  choice가 6이면 "게임을 종료합니다. 안녕히 가세요!" 출력 후 break.
  1~5면 "아직 준비 중인 기능입니다." 출력.
  except (KeyboardInterrupt, EOFError): 로 잡아
  "\n⚠️ 입력이 중단되었습니다. 안전하게 종료합니다." 출력 후 끝낸다.

(2) main.py — 시작점
- from game import QuizGame
- if __name__ == "__main__": 안에서 game = QuizGame() 을 만들고 game.run() 을 호출한다.
  이 관용구가 무슨 뜻인지 주석으로 설명한다.

동작 예시(정확히 이렇게 되어야 한다):
선택: abc → ⚠️ 숫자를 입력하세요.
선택: 9 → ⚠️ 1~6 사이의 숫자를 입력하세요.
선택: (그냥 엔터) → ⚠️ 아무것도 입력되지 않았습니다. 다시 입력하세요.
선택: 1 → 아직 준비 중인 기능입니다. (그리고 메뉴가 다시 나온다)
선택: 6 → 게임을 종료합니다. 안녕히 가세요!
```

**검수 포인트:** ⑴ 클래스·메서드 이름이 `QuizGame`, `show_menu`, `get_number_input`, `run` 그대로인지
⑵ `run`의 try가 while 전체를 감싸는지 ⑶ main.py에 `if __name__ == "__main__":`이 있는지 ⑷ 메뉴가 6번(종료)까지인지.

**실행 확인:**

```
python3 main.py
```
> ① 프로그램 실행 ② 메뉴가 뜨고, `abc`·`9`·빈 엔터·`Ctrl+C`·`6`을 각각 시험했을 때 지시문의 동작 예시와 동일 ③ 다르면 9-2의 오류 수리 지시문 사용

**기록:**

```
git add .
```
> ① 변경 담기 ② 출력 없음 ③ 카드 13

```
git commit -m "Feat: QuizGame 뼈대와 메뉴, 번호 입력 검증 구현"
```
> ① 커밋 ② 생성 ② `[main ...] Feat: ...` ③ 카드 14

```
git push
```
> ① 깃허브로 올림 ② 아이디·토큰 입력 후 업로드 요약 ③ 카드 15

**시험 대비 질문 3개 (네이토에게 그대로 묻기):**
"while True와 break는 어떻게 짝을 이루나?" / "try/except ValueError는 정확히 어떤 순간을 잡아내나?" /
"if __name__ == '__main__' 이 왜 필요한가? 없으면 무슨 일이 생기나?"

---

### 3단계. Quiz 클래스 — quiz.py (커밋 ③)

**네이토 지시문 P2 (그대로 복사)**

```
[3단계] quiz.py 파일을 새로 만든다. 파일 전체를 출력하라. game.py와 main.py는 이번에 수정하지 않는다.

Quiz 클래스 사양:
- __init__(self, question, choices, answer, hint=None)
  self.question(문자열), self.choices(문자열 4개 리스트), self.answer(정수 1~4),
  self.hint(문자열 또는 None). 각 속성 옆에 자료형과 뜻을 주석으로 단다.
  hint=None 처럼 기본값을 두는 이유(힌트 없는 퀴즈도 허용)를 주석으로 설명한다.
- display(self, number=None):
  number가 None이 아니면 f"[문제 {number}]"를 출력 → self.question 출력 → 빈 줄 →
  enumerate(self.choices, start=1)로 "1. 선택지" 형태로 출력 → 빈 줄.
- check_answer(self, user_answer): user_answer == self.answer 의 결과(True/False)를 반환한다.
- to_dict(self): {"question": ..., "choices": ..., "answer": ..., "hint": ...} 딕셔너리를 반환한다.
  JSON 파일 저장용이라는 주석을 단다.
- @classmethod
  from_dict(cls, data): 딕셔너리를 받아
  cls(data["question"], data["choices"], data["answer"], data.get("hint")) 로
  Quiz 객체를 만들어 반환한다. data.get("hint")를 쓰는 이유(열쇠가 없어도 오류 대신 None)를 주석으로 설명한다.

파일 맨 아래 자체 시험 코드:
if __name__ == "__main__": 안에서 예시 Quiz 하나를 만들어 display(1) 호출,
check_answer(2) 결과 출력, to_dict() 결과 출력.
```

**검수 포인트:** ⑴ 속성 4개 이름 그대로 ⑵ `from_dict` 위에 `@classmethod`가 붙어 있는지
⑶ `data.get("hint")` 사용 여부 ⑷ 파일 아래 자체 시험 블록 존재. (부록 B의 참조 코드와 비교해도 됩니다.)

**실행 확인:**

```
python3 quiz.py
```
> ① quiz.py 단독 자체 시험 ② 문제·선택지 4줄·True·딕셔너리 출력 ③ 오류면 화면 복사 후 오류 수리 지시문 (카드 없음: 파이썬 실행)

```
python3 main.py
```
> ① 본 게임이 여전히 정상인지 확인 ② 2단계와 동일하게 동작(6으로 종료) ③ 오류 수리 지시문

**기록:**

```
git add .
```
> ① 담기 ② 없음 ③ 카드 13

```
git commit -m "Feat: Quiz 클래스 구현 (문제 표시, 정답 확인, 딕셔너리 변환)"
```
> ① 커밋 ③ ② `[main ...]` ③ 카드 14

```
git push
```
> ① 올리기 ② 업로드 요약 ③ 카드 15

**시험 대비 질문 3개:** "self는 정확히 무엇을 가리키나?" / "__init__은 언제, 몇 번 실행되나?" /
"@classmethod가 붙은 메서드와 일반 메서드는 부르는 방법이 어떻게 다른가?"

---

### 4단계. 기본 퀴즈 5개 — game.py 수정 (커밋 ④)

**네이토 지시문 P3 (그대로 복사)**

```
[4단계] game.py를 수정한다. 수정된 game.py 파일 전체를 출력하라.

수정 내용:
1. 파일 맨 위에 from quiz import Quiz 를 추가한다. (quiz.py에서 Quiz 클래스를 가져온다는 주석)
2. QuizGame에 get_default_quizzes(self) 메서드를 추가한다. 아래 5개 퀴즈를 Quiz(...) 객체로
   만들어 리스트로 반환한다. 문제·선택지·정답·힌트를 한 글자도 바꾸지 말 것.
  1) 문제 "파이썬의 창시자는?" / 선택지 ["리누스 토르발스", "귀도 반 로섬", "제임스 고슬링", "브렌던 아이크"] / 정답 2 / 힌트 "네덜란드 출신입니다."
  2) 문제 "변경 내용을 기록 한 장으로 확정해 저장하는 깃 명령은?" / 선택지 ["add", "push", "commit", "clone"] / 정답 3 / 힌트 "'확정하다'라는 뜻의 영어 단어입니다."
  3) 문제 "파이썬에서 리스트를 만들 때 쓰는 괄호는?" / 선택지 ["( )", "[ ]", "{ }", "< >"] / 정답 2 / 힌트 "모서리가 각진 대괄호입니다."
  4) 문제 "원격 저장소의 새 변경 사항을 내 컴퓨터로 가져오는 깃 명령은?" / 선택지 ["pull", "push", "merge", "init"] / 정답 1 / 힌트 "'당겨온다'는 뜻입니다."
  5) 문제 "이 게임이 데이터를 저장할 때 쓰는 파일 형식은?" / 선택지 ["TXT", "CSV", "XML", "JSON"] / 정답 4 / 힌트 "자바스크립트 객체 표기법의 줄임말입니다."
3. __init__에서 self.quizzes = self.get_default_quizzes() 로 바꾼다.
   (나중 단계에서 이 자리가 "파일에서 불러오기"로 교체될 예정이라는 주석을 단다)
나머지 메서드와 메뉴는 그대로 유지한다.
```

- 주제를 바꾸고 싶으면: 위 5개 퀴즈만 본인 주제(각각 문제·선택지 4개·정답 번호·힌트)로 갈아 끼우면 됩니다.
  **정답 번호가 특정 번호에 몰리지 않게** 하고, 사실이 확실한 문제만 씁니다.

**검수 포인트:** ⑴ `from quiz import Quiz` ⑵ 퀴즈 5개의 정답 번호가 2, 3, 2, 1, 4 그대로인지
⑶ `__init__`이 `get_default_quizzes()`를 쓰는지.

**실행 확인:**

```
python3 main.py
```
> ① 정상 구동 확인 ② 화면 변화는 아직 없음(데이터는 5단계 "퀴즈 풀기"에서 눈으로 확인됨). 2단계와 동일하게 동작하면 통과 ③ 오류 수리 지시문

**기록:**

```
git add .
```
> ① 담기 ② 없음 ③ 카드 13

```
git commit -m "Feat: 기본 퀴즈 데이터 5개 등록"
```
> ① 커밋 ④ ② `[main ...]` ③ 카드 14

```
git push
```
> ① 올리기 ② 업로드 요약 ③ 카드 15

**시험 대비 질문 3개:** "객체 5개를 리스트에 담아 두면 무엇이 편해지나?" /
"from quiz import Quiz 는 정확히 무엇을 가져오는 건가?" /
"퀴즈 데이터를 코드에 직접 쓰는 방식의 장점과 한계는?"

---

### 5단계. 브랜치에서 퀴즈 풀기 만들고 병합 (커밋 ⑤·⑥) — 이번 단계만 순서가 특별합니다

**5-A. 먼저 가지를 만들어 갈아탑니다.**

```
git status
```
> ① 깨끗한 상태인지 확인 ② `working tree clean` ③ 변경이 남았으면 4단계 커밋부터 (카드 8)

```
git checkout -b feature/quiz-play
```
> ① "feature/quiz-play" 가지를 새로 만들며 이동 ② `Switched to a new branch 'feature/quiz-play'` ③ 카드 16

```
git branch
```
> ① 현재 가지 확인 ② `* feature/quiz-play` 에 별표 ③ 별표 위치가 다르면 카드 16으로 이동 (카드 11)

**5-B. 네이토 지시문 P4 (그대로 복사)**

```
[5단계] game.py를 수정한다. 수정된 game.py 파일 전체를 출력하라.

수정 내용: play_quiz(self) 메서드를 새로 만들고, run()에서 choice가 1일 때
self.play_quiz()를 호출하도록 바꾼다. (1번 자리의 "아직 준비 중" 문구는 제거)

play_quiz(self) 사양:
1. self.quizzes가 비어 있으면 "⚠️ 등록된 퀴즈가 없습니다. 먼저 퀴즈를 추가하세요." 출력 후 반환한다.
2. f"\n📝 퀴즈를 시작합니다! (총 {문제 수}문제)" 출력.
3. 모든 퀴즈를 저장된 순서대로 출제한다(무작위 섞기는 나중 단계).
   enumerate(self.quizzes, start=1) 를 사용한다. 각 문제마다:
   "-" 40개 구분선 출력 → quiz.display(번호) →
   answer = self.get_number_input("정답 입력 (1~4): ", 1, 4) →
   quiz.check_answer(answer)가 True면 "✅ 정답입니다!" 출력하고 맞힌 수를 1 늘린다.
   False면 f"❌ 오답입니다. 정답은 {quiz.answer}번입니다." 출력.
4. 다 풀면 "=" 40개 출력 → score = round(맞힌 수 / 문제 수 * 100) 계산 →
   f"🏆 결과: {문제 수}문제 중 {맞힌 수}문제 정답! ({score}점)" 출력 → "=" 40개 출력.
   (최고 점수 비교는 나중 단계에서 추가한다는 주석)
다른 메서드와 메뉴는 그대로 유지한다.
```

**검수 포인트:** ⑴ `play_quiz` 이름 ⑵ 퀴즈 없음 처리 ⑶ 점수 계산이 `round(맞힌 수/문제 수*100)`인지
⑷ run의 1번 분기가 play_quiz 호출로 바뀌었는지.

**실행 확인:**

```
python3 main.py
```
> ① 메뉴에서 1 선택 ② 5문제가 차례로 나오고, 다 풀면 `🏆 결과: 5문제 중 N문제 정답! (M점)` ③ 오류 수리 지시문

**5-C. 가지 위에서 커밋하고 가지도 올려 둡니다(공용 맥 백업).**

```
git add .
```
> ① 담기 ② 없음 ③ 카드 13

```
git commit -m "Feat: 퀴즈 풀기 기능 구현"
```
> ① 커밋 ⑤ (가지 위에 생성) ② `[feature/quiz-play ...]` ③ 카드 14

```
git push -u origin feature/quiz-play
```
> ① 이 가지를 깃허브에 처음 올림 ② 업로드 요약 + 가지 연결 문구 ③ 카드 15

**5-D. 본줄기로 돌아와 병합합니다. 병합은 단독 동작입니다.**

```
git checkout main
```
> ① main으로 복귀 ② `Switched to branch 'main'` ③ 카드 16

```
git branch
```
> ① 현재 가지 재확인 ② `* main` ③ 별표가 main이 아니면 병합 금지, 카드 16으로 (카드 11)

```
git merge --no-ff feature/quiz-play -m "Merge: 퀴즈 풀기 브랜치 병합"
```
> ① 가지 작업을 main에 합치고 병합 커밋 ⑥ 생성 ② `Merge made by ...` + 파일 요약 ③ `CONFLICT`가 보이면 즉시 카드 21로 취소 후 문의 (카드 17)

```
git push
```
> ① 병합된 main을 올림 ② 업로드 요약 ③ 카드 15

```
git log --oneline --graph
```
> ① 가지가 갈라졌다 합쳐진 그림 확인(제출용 화면과 동일) ② 선이 갈라졌다 만나는 모양 + 커밋 6개 ③ 화면이 멈춘 듯하면 `q`로 나가기 (카드 20)

**시험 대비 질문 3개:** "브랜치를 쓰면 무엇이 안전해지나?" / "병합 커밋은 보통 커밋과 무엇이 다른가?" /
"enumerate(리스트, start=1)은 무엇을 돌려주나?"

---

### 6단계. 퀴즈 추가 (커밋 ⑦)

**네이토 지시문 P5 (그대로 복사)**

```
[6단계] game.py를 수정한다. 수정된 game.py 파일 전체를 출력하라.

수정 내용 1: 글자 입력용 보조 메서드 get_text_input(self, prompt)를 추가한다.
while True 반복 안에서 input(prompt)로 받아 .strip() 처리하고,
빈 문자열이면 "⚠️ 아무것도 입력되지 않았습니다. 다시 입력하세요." 출력 후 반복,
내용이 있으면 그 문자열을 반환한다.

수정 내용 2: add_quiz(self) 메서드를 추가하고, run()에서 choice가 2일 때 호출한다.
1. "\n📌 새로운 퀴즈를 추가합니다." 출력.
2. question = self.get_text_input("문제를 입력하세요: ")
3. for 반복으로 선택지 4개를 받아 리스트 choices에 모은다.
   각각 self.get_text_input(f"선택지 {i}: ") 를 쓴다. (i는 1~4)
4. answer = self.get_number_input("정답 번호 (1~4): ", 1, 4)
5. Quiz(question, choices, answer) 객체를 만들어 self.quizzes에 append 한다.
   (힌트 입력은 나중 단계에서 추가한다는 주석)
6. f"✅ 퀴즈가 추가되었습니다! (현재 {len(self.quizzes)}개)" 출력.
   (파일 저장 연결은 나중 단계라는 주석)
다른 메서드는 그대로 유지한다.
```

**검수 포인트:** ⑴ `get_text_input` 추가 ⑵ 선택지 4개를 for로 받는지 ⑶ `append` 사용 ⑷ run의 2번 분기 연결.

**실행 확인:** 실행 → 2번으로 퀴즈 하나 추가 → 1번 풀기에서 총 6문제가 나오면 통과.
(아직 저장 기능이 없어 껐다 켜면 사라지는 것이 이 단계의 정상 동작입니다. 9단계에서 해결됩니다.)

```
git add .
```
> ① 담기 ② 없음 ③ 카드 13

```
git commit -m "Feat: 퀴즈 추가 기능 구현"
```
> ① 커밋 ⑦ ② `[main ...]` ③ 카드 14

```
git push
```
> ① 올리기 ② 업로드 요약 ③ 카드 15

**시험 대비 질문 3개:** "append는 리스트를 어떻게 바꾸나?" / "for i in range(1, 5)는 몇 번 도나?" /
"입력 검증을 보조 메서드로 빼면 무엇이 좋아지나?"

---

### 7단계. 퀴즈 목록 (커밋 ⑧)

**네이토 지시문 P6 (그대로 복사)**

```
[7단계] game.py를 수정한다. 수정된 game.py 파일 전체를 출력하라.

수정 내용: show_quiz_list(self) 메서드를 추가하고, run()에서 choice가 3일 때 호출한다.
1. self.quizzes가 비어 있으면 "⚠️ 등록된 퀴즈가 없습니다." 출력 후 반환한다.
2. f"\n📋 등록된 퀴즈 목록 (총 {len(self.quizzes)}개)" 출력 → "-" 40개 구분선 출력.
3. enumerate(self.quizzes, start=1) 로 f"[{i}] {quiz.question}" 을 한 줄씩 출력한다.
   (선택지와 정답은 일부러 보여주지 않는다: 정답 유출 방지라는 주석)
4. "-" 40개 구분선 출력.
다른 메서드는 그대로 유지한다.
```

**검수 포인트:** ⑴ 빈 목록 처리 ⑵ 문제만 출력(정답 미노출) ⑶ run의 3번 분기 연결.

**실행 확인:** 실행 → 3번 → `📋 등록된 퀴즈 목록 (총 5개)`와 문제 5줄이 나오면 통과.

```
git add .
```
> ① 담기 ② 없음 ③ 카드 13

```
git commit -m "Feat: 퀴즈 목록 기능 구현"
```
> ① 커밋 ⑧ ② `[main ...]` ③ 카드 14

```
git push
```
> ① 올리기 ② 업로드 요약 ③ 카드 15

**시험 대비 질문 3개:** "quiz.question처럼 점(.)으로 속성을 꺼내는 원리는?" /
"len()은 어떤 자료형에 쓸 수 있나?" / "목록에서 정답을 숨긴 것은 어떤 설계 사고방식인가?"

---

### 8단계. 점수 확인 — 최고 점수 (커밋 ⑨)

**네이토 지시문 P7 (그대로 복사)**

```
[8단계] game.py를 수정한다. 수정된 game.py 파일 전체를 출력하라.

수정 내용 1: __init__에 self.best_score = None 을 추가한다.
(None은 "아직 기록이 없다"는 뜻이고, 0점과 구분하려고 None을 쓴다는 주석)

수정 내용 2: play_quiz의 결과 출력 뒤에 최고 점수 갱신을 추가한다.
if self.best_score is None or score > self.best_score: 일 때
self.best_score = score 로 갱신하고 "🎉 새로운 최고 점수입니다!" 를 출력한다.

수정 내용 3: show_score(self) 메서드를 추가하고, run()에서 choice가 5일 때 호출한다.
self.best_score가 None이면 "⚠️ 아직 퀴즈를 푼 기록이 없습니다." 출력,
아니면 f"\n🏆 최고 점수: {self.best_score}점" 출력.
다른 메서드는 그대로 유지한다.
```

**검수 포인트:** ⑴ `None` 초기화 ⑵ `is None` 비교 사용 ⑶ 갱신 시 축하 문구 ⑷ run의 5번 분기 연결.

**실행 확인:** 실행 직후 5번 → "아직 퀴즈를 푼 기록이 없습니다." / 1번으로 한 판 푼 뒤 5번 → 점수 표시.

```
git add .
```
> ① 담기 ② 없음 ③ 카드 13

```
git commit -m "Feat: 점수 확인 기능 구현 (최고 점수 갱신)"
```
> ① 커밋 ⑨ ② `[main ...]` ③ 카드 14

```
git push
```
> ① 올리기 ② 업로드 요약 ③ 카드 15

**시험 대비 질문 3개:** "None과 0은 무엇이 다른가?" / "== None이 아니라 is None을 권하는 이유는?" /
"or 조건은 왼쪽이 참이면 오른쪽을 건너뛰는데, 이 코드에서 그게 왜 중요한가?"

---

### 9단계. state.json 저장·불러오기 (커밋 ⑩) — 데이터 영속성의 심장

**네이토 지시문 P8 (그대로 복사)**

```
[9단계] game.py를 수정한다. 수정된 game.py 파일 전체를 출력하라.

수정 내용 1: 파일 맨 위에 import json 을 추가한다.

수정 내용 2: save_state(self) 메서드를 추가한다.
1. data = {"quizzes": [q.to_dict() for q in self.quizzes], "best_score": self.best_score}
   (대괄호 안 for 문이 "리스트 컴프리헨션"이며, 각 Quiz 객체를 딕셔너리로 바꿔
   새 리스트를 만든다는 주석)
2. try 안에서 with open("state.json", "w", encoding="utf-8") as f: 로 열고
   json.dump(data, f, ensure_ascii=False, indent=2) 로 저장한다.
   (ensure_ascii=False는 한글이 깨지지 않게, indent=2는 사람이 읽기 좋게라는 주석)
3. except OSError: 로 잡아 "⚠️ 저장 중 문제가 발생했습니다." 출력.

수정 내용 3: load_state(self) 메서드를 추가한다.
1. try 안에서 with open("state.json", "r", encoding="utf-8") as f: 로 열고 data = json.load(f).
   self.quizzes = [Quiz.from_dict(d) for d in data["quizzes"]]
   self.best_score = data["best_score"]
   f"📂 저장된 데이터를 불러왔습니다. (퀴즈 {len(self.quizzes)}개)" 출력.
2. except FileNotFoundError: → 첫 실행이라는 뜻. self.quizzes = self.get_default_quizzes(),
   self.best_score = None 으로 두고 "📂 저장된 파일이 없어 기본 퀴즈로 시작합니다." 출력.
3. except (json.JSONDecodeError, KeyError, TypeError): → 파일이 손상됐다는 뜻.
   self.quizzes = self.get_default_quizzes(), self.best_score = None 으로 복구하고
   "⚠️ 저장 파일이 손상되어 기본 퀴즈로 다시 시작합니다." 출력.

수정 내용 4: __init__을 고친다. self.quizzes = [] 와 self.best_score = None 을 먼저 두고,
마지막 줄에서 self.load_state() 를 호출한다. (get_default_quizzes 직접 호출은 제거)

수정 내용 5: 저장 시점을 연결한다.
- add_quiz 마지막에 self.save_state() 호출.
- play_quiz에서 최고 점수가 갱신된 경우 self.save_state() 호출.
- run에서 6(종료) 선택 시 self.save_state() 호출 후 "💾 저장했습니다." 출력하고 인사 후 break.
- run의 except (KeyboardInterrupt, EOFError) 처리에서도 self.save_state() 호출 후
  "\n⚠️ 입력이 중단되었습니다. 데이터를 저장하고 안전하게 종료합니다." 출력.
다른 메서드는 그대로 유지한다.
```

**검수 포인트:** ⑴ `import json` ⑵ `ensure_ascii=False`와 `encoding="utf-8"` ⑶ 예외 3갈래(파일 없음/손상/저장 실패)
⑷ `__init__` 마지막이 `self.load_state()` ⑸ 저장 호출 4곳(추가·최고점 갱신·종료·중단).

**실행 확인 (3가지 시험):**
- **유지 시험:** 실행 → 퀴즈 1개 추가 → 6으로 종료 → 다시 실행 → "📂 저장된 데이터를 불러왔습니다. (퀴즈 6개)" → 3번 목록에 6개.
- **파일 확인:**

```
cat state.json
```
> ① 저장된 데이터 눈으로 확인 ② 2-3장의 예시와 같은 모양의 JSON(한글이 그대로 보여야 함) ③ 한글이 `\uXXXX`로 보이면 `ensure_ascii=False` 누락 → 오류 수리 지시문 (카드 5)

- **손상 시험:** VS Code로 state.json을 열어 맨 앞에 아무 글자(예: x)를 넣고 저장 → 실행 →
  "⚠️ 저장 파일이 손상되어 기본 퀴즈로 다시 시작합니다."가 나오고 기본 5개로 동작하면 통과.
  (한 번 저장 동작이 일어나면 파일은 다시 정상으로 덮어써집니다. 추가했던 퀴즈가 사라지는 것은
  미션이 허용한 "초기화 복구" 방식이라 정상입니다.)

```
git add .
```
> ① 담기 ② 없음 ③ 카드 13

```
git commit -m "Feat: state.json 저장·불러오기 구현 (손상 복구 포함)"
```
> ① 커밋 ⑩ ② `[main ...]` ③ 카드 14

```
git push
```
> ① 올리기 ② 업로드 요약 ③ 카드 15

**시험 대비 질문 3개:** "with open을 쓰면 무엇이 자동으로 처리되나?" /
"ensure_ascii=False를 빼면 한글이 어떻게 저장되나?" / "예외를 종류별로 나눠 잡는 이유는?"

---

### 10단계. 보너스: 랜덤 출제 + 문제 수 선택 (커밋 ⑪)

두 보너스는 모두 `play_quiz` 한 곳만 바꾸므로 한 단계로 묶었습니다(과쪼개기 방지).

**네이토 지시문 P9 (그대로 복사)**

```
[10단계] game.py를 수정한다. 수정된 game.py 파일 전체를 출력하라.

수정 내용 1: 파일 맨 위에 import random 을 추가한다.

수정 내용 2: play_quiz를 고친다. 퀴즈 없음 검사 바로 다음에 아래를 넣는다.
count = self.get_number_input(f"몇 문제를 풀까요? (1~{len(self.quizzes)}): ", 1, len(self.quizzes))
selected = random.sample(self.quizzes, count)
(random.sample은 겹치지 않게 count개를 무작위로 뽑고, 뽑힌 순서 자체가 섞여 있어서
"무작위 출제"와 "문제 수 선택"을 한 번에 해결한다는 주석.
random.shuffle과의 차이 — shuffle은 원본 리스트 자체를 섞는다 — 도 주석 한 줄로 설명.)
이후 출제 반복은 self.quizzes 대신 selected를 사용하고,
문제 수 안내와 점수 계산도 count 기준으로 바꾼다.
다른 메서드는 그대로 유지한다.
```

**검수 포인트:** ⑴ `import random` ⑵ `random.sample(self.quizzes, count)` ⑶ 반복·점수 계산이 `selected`/`count` 기준.

**실행 확인:** 1번 → "몇 문제를 풀까요? (1~5)" → 3 입력 → 3문제만, 실행할 때마다 순서가 다르면 통과.

```
git add .
```
> ① 담기 ② 없음 ③ 카드 13

```
git commit -m "Feat: 랜덤 출제와 문제 수 선택 추가"
```
> ① 커밋 ⑪ ② `[main ...]` ③ 카드 14

```
git push
```
> ① 올리기 ② 업로드 요약 ③ 카드 15

**시험 대비 질문 3개:** "sample과 shuffle의 차이는?" / "sample이 원본을 바꾸지 않는 걸 어떻게 확인하나?" /
"표준 라이브러리란 무엇이고 외부 라이브러리와 뭐가 다른가?"

---

### 11단계. 보너스: 힌트 기능 (커밋 ⑫)

**네이토 지시문 P10 (그대로 복사)**

```
[11단계] game.py를 수정한다. 수정된 game.py 파일 전체를 출력하라.

수정 내용 1: play_quiz의 각 문제 정답 입력 부분을 아래 규칙으로 바꾼다.
- 출제 반복 시작 전에 hints_used = 0 을 준비한다(이번 판에서 힌트를 본 횟수).
- 각 문제에서 while True 반복:
  answer = self.get_number_input("정답 입력 (1~4, 힌트 보기는 0): ", 0, 4)
  answer가 0이면: quiz.hint가 None이 아니면 f"💡 힌트: {quiz.hint}" 출력하고 hints_used를 1 늘린다.
  quiz.hint가 None이면 "💡 이 문제에는 힌트가 없습니다." 출력(횟수는 늘리지 않음).
  두 경우 모두 다시 입력을 받는다.
  answer가 1~4면 반복을 나가서 기존 방식대로 채점한다.
- 점수 계산을 score = max(0, round(맞힌 수 / count * 100) - hints_used * 5) 로 바꾼다.
  결과 출력에서 hints_used가 1 이상이면
  f"   (힌트 {hints_used}회 사용, {hints_used * 5}점 차감)" 한 줄을 추가로 출력한다.

수정 내용 2: add_quiz에서 정답 번호 입력 다음에 힌트를 받는다.
hint = input("힌트를 입력하세요 (없으면 그냥 Enter): ").strip()
빈 문자열이면 hint = None 으로 바꾼다.
(여기서는 빈 입력이 "힌트 없음"이라는 정상 입력이므로 get_text_input을 쓰지 않는다는 주석)
Quiz를 만들 때 hint를 함께 넘긴다.
다른 메서드는 그대로 유지한다.
```

**검수 포인트:** ⑴ 입력 범위가 0~4로 바뀜 ⑵ 힌트 없음 처리 ⑶ `max(0, ...)` 차감식 ⑷ add_quiz에 힌트 입력.

**실행 확인:** 문제에서 0 입력 → 힌트 출력 후 다시 물음 → 정답 맞히고 판 종료 시 차감 문구가 보이면 통과.

```
git add .
```
> ① 담기 ② 없음 ③ 카드 13

```
git commit -m "Feat: 힌트 기능 추가 (0으로 보기, 점수 차감)"
```
> ① 커밋 ⑫ ② `[main ...]` ③ 카드 14

```
git push
```
> ① 올리기 ② 업로드 요약 ③ 카드 15

**시험 대비 질문 3개:** "max(0, 점수)가 막아 주는 상황은?" / "0을 '힌트 보기' 신호로 쓰는 설계의 장단점은?" /
"None인지 확인할 때 if quiz.hint: 와 if quiz.hint is not None: 은 어떻게 다른가?"

---

### 12단계. 보너스: 퀴즈 삭제 (커밋 ⑬)

**네이토 지시문 P11 (그대로 복사)**

```
[12단계] game.py를 수정한다. 수정된 game.py 파일 전체를 출력하라.

수정 내용: delete_quiz(self) 메서드를 추가하고, run()에서 choice가 4일 때 호출한다.
1. self.quizzes가 비어 있으면 "⚠️ 등록된 퀴즈가 없습니다." 출력 후 반환한다.
2. self.show_quiz_list() 를 호출해 목록을 먼저 보여준다(이미 만든 메서드 재사용 주석).
3. num = self.get_number_input("🗑️ 삭제할 퀴즈 번호: ", 1, len(self.quizzes))
4. target = self.quizzes[num - 1] 을 잡고 f"선택한 퀴즈: {target.question}" 을 보여준 뒤
   ok = input("정말 삭제할까요? (y/n): ").strip().lower()
   ok가 "y"일 때만 self.quizzes.pop(num - 1) → self.save_state() → "✅ 삭제했습니다." 출력.
   그 외의 모든 입력은 "취소했습니다." 출력.
   (삭제는 되돌릴 수 없어 확인을 거치며, y가 아니면 전부 취소로 처리하는 것이
   안전한 기본값이라는 주석. 화면 번호는 1부터, 리스트 자리는 0부터라서
   num - 1 이 필요하다는 주석)
다른 메서드는 그대로 유지한다.
```

**검수 포인트:** ⑴ 목록 재사용 ⑵ y/n 확인 ⑶ `pop(num - 1)` ⑷ 삭제 후 `save_state()` 호출.

**실행 확인:** 4번 → 목록 → 번호 → n 입력 시 취소 / y 입력 시 삭제 → 3번 목록에서 개수 감소 →
종료 후 재실행해도 줄어든 상태가 유지되면 통과.

```
git add .
```
> ① 담기 ② 없음 ③ 카드 13

```
git commit -m "Feat: 퀴즈 삭제 기능 추가 (확인 절차 포함)"
```
> ① 커밋 ⑬ ② `[main ...]` ③ 카드 14

```
git push
```
> ① 올리기 ② 업로드 요약 ③ 카드 15

**시험 대비 질문 3개:** "번호와 인덱스가 1 어긋나는 실수는 어떤 오류로 나타나나?" /
"pop과 remove는 무엇이 다른가?" / "왜 y 이외의 모든 입력을 취소로 처리하는 편이 안전한가?"

---

### 13단계. 보너스: 점수 기록 히스토리 (커밋 ⑭)

**네이토 지시문 P12 (그대로 복사)**

```
[13단계] game.py를 수정한다. 수정된 game.py 파일 전체를 출력하라.

수정 내용 1: 파일 맨 위에 import datetime 을 추가한다.

수정 내용 2: __init__에 self.history = [] 를 추가한다(게임 기록 목록).

수정 내용 3: save_state의 data 딕셔너리에 "history": self.history 를 추가한다.

수정 내용 4: load_state의 정상 처리에서 self.history = data.get("history", []) 로 읽는다.
(예전에 저장된 파일에는 history 열쇠가 없을 수 있으므로 .get으로 기본값을 준다는 주석.
불러오기 안내 문구는
f"📂 저장된 데이터를 불러왔습니다. (퀴즈 {len(self.quizzes)}개, 기록 {len(self.history)}회)"
로 바꾼다.) 첫 실행·손상 처리에서는 self.history = [] 로 둔다.

수정 내용 5: play_quiz의 점수 계산 뒤에 기록을 남긴다.
now = datetime.datetime.now().strftime("%Y-%m-%d %H:%M")
self.history.append({"date": now, "total": count, "correct": 맞힌 수, "score": score})
그리고 최고 점수 갱신 여부와 관계없이 self.save_state() 를 호출한다(기록은 매판 저장이라는 주석).

수정 내용 6: show_score를 고친다. 최고 점수 표시(기존 규칙 유지) 아래에,
self.history가 비어 있지 않으면 "📜 최근 게임 기록 (최신순)" 을 출력하고
최근 5개를 최신부터 f"- {r['date']} | {r['total']}문제 중 {r['correct']}개 정답 | {r['score']}점"
형태로 출력한다. (리스트를 뒤에서부터 읽는 방법을 주석으로 설명)
다른 메서드는 그대로 유지한다.
```

**검수 포인트:** ⑴ `import datetime` ⑵ `data.get("history", [])` ⑶ 매판 저장 ⑷ 최신순 5개 표시.

**실행 확인:** 두세 판 플레이 → 5번 → 최고 점수와 함께 날짜·문제 수·점수 기록이 최신순으로 보이고,
재실행 후에도 기록이 남아 있으면 통과. `cat state.json`으로 history 항목도 확인.

```
git add .
```
> ① 담기 ② 없음 ③ 카드 13

```
git commit -m "Feat: 점수 기록 히스토리 추가"
```
> ① 커밋 ⑭ ② `[main ...]` ③ 카드 14

```
git push
```
> ① 올리기 ② 업로드 요약 ③ 카드 15

**시험 대비 질문 3개:** "strftime의 %Y %m %d %H %M은 각각 무엇인가?" /
"data.get('history', [])의 두 번째 인자는 언제 쓰이나?" /
"저장 파일의 구조(스키마)가 늘어날 때 옛 파일과의 호환은 어떻게 지키나?"

---

## 11장. 14단계 — 다듬기(리팩터)와 README 완성 (커밋 ⑮·⑯)

### 14-A. 진짜 리팩터 커밋 만들기

**네이토 지시문 P13 (그대로 복사)**

```
[14단계] game.py를 수정한다. 수정된 game.py 파일 전체를 출력하라.
이번 단계는 "리팩터링"이다. 동작(화면 문구·기능)은 단 하나도 바뀌면 안 된다.

수정 내용:
1. 파일 맨 위 import 줄들 아래에 상수 3개를 만든다.
   (상수 이름은 대문자로 쓰는 것이 파이썬 관례라는 주석과 함께)
   DATA_FILE = "state.json"        # 데이터 파일 이름. 한 곳만 바꾸면 전체에 적용된다.
   HINT_PENALTY = 5                # 힌트 1회당 차감 점수
   RECENT_HISTORY_COUNT = 5        # 점수 화면에 보여줄 최근 기록 개수
2. 본문에서 "state.json"이라는 글자, 힌트 차감에 쓰인 숫자 5, 최근 기록 개수 5를
   직접 쓴 곳을 전부 위 상수로 바꾼다.
3. 모든 메서드를 점검해서, 각 메서드 첫 줄에 역할 한 줄 요약 주석이 빠짐없이 있게 한다.
기능·출력 문구·클래스와 메서드 이름은 절대 바꾸지 않는다.
```

**검수 포인트:** ⑴ 상수 3개가 파일 위쪽에 있음 ⑵ 본문에 `"state.json"` 문자가 상수 정의 외에는 없음
⑶ 실행 화면이 13단계와 완전히 동일함(리팩터의 정의!).

**실행 확인:** 한 판 플레이 + 저장 + 재실행 → 13단계와 똑같이 동작하면 통과.

```
git add .
```
> ① 담기 ② 없음 ③ 카드 13

```
git commit -m "Refactor: 상수 분리와 주석 정리"
```
> ① 커밋 ⑮ ② `[main ...]` ③ 카드 14

```
git push
```
> ① 올리기 ② 업로드 요약 ③ 카드 15

### 14-B. README 완성

VS Code에서 `README.md`를 열고, **부록 C의 전문을 통째로 붙여넣어 내용을 교체**합니다.
그다음 부록 C 안의 `[빈칸]` 두 곳(깃허브 아이디, 주제 선정 이유)을 본인 것으로 채웁니다.
선정 이유는 꼭 **본인의 말**로 다듬으세요. 채점자와 시험 모두에서 "자기 언어"가 중요합니다.

```
git add .
```
> ① 담기 ② 없음 ③ 카드 13

```
git commit -m "Docs: README 상세 작성"
```
> ① 커밋 ⑯ ② `[main ...]` ③ 카드 14

```
git push
```
> ① 올리기 ② 업로드 요약 ③ 카드 15

깃허브 웹에서 저장소를 새로고침해 README가 예쁘게 표시되는지 확인합니다.

---

## 12장. 15단계 — 복제(clone)·당겨오기(pull) 실습 (커밋 ⑰)

미션이 요구하는 마지막 깃 경험입니다. **내 저장소를 다른 폴더에 복제**해서 수정하고,
그 변경을 **원래 폴더에서 당겨오는** 흐름입니다.
주의: 이 실습 동안 **원래 폴더(python-quiz-game)에서는 아무 커밋도 만들지 않습니다**(충돌 예방).

```
cd ~/Desktop
```
> ① 바탕화면으로 이동(프로젝트 폴더 밖) ② 출력 없음 ③ 카드 3

```
git clone https://github.com/내아이디/python-quiz-game.git python-quiz-game-clone
```
> ① 내 저장소를 "python-quiz-game-clone" 폴더로 복제 (**내아이디 교체**) ② `Cloning into ... done.` ③ 인증 실패면 아이디·토큰 재입력 (카드 19)

```
cd python-quiz-game-clone
```
> ① 복제 폴더로 진입 ② 출력 없음 ③ 카드 3

복제 폴더는 **새 저장소**라서 공용 맥 보호 설정을 다시 해야 합니다.

```
git config user.name "본인이름"
```
> ① 이 복제 저장소의 작성자 이름 설정 ② 출력 없음 ③ 카드 9

```
git config user.email "깃허브가입이메일"
```
> ① 이메일 설정 ② 출력 없음 ③ 카드 9

```
git config credential.helper ""
```
> ① 로그인 정보 저장 끔 ② 출력 없음 ③ 카드 22

```
python3 main.py
```
> ① 복제본이 어디서든 똑같이 도는지 확인 ② 메뉴가 뜸(마지막으로 커밋된 state.json 기준 데이터) ③ 오류면 화면 복사 후 문의

이제 VS Code로 이 **복제 폴더의** `README.md`를 열어, 맨 아래에 한 줄을 추가하고 저장합니다.

```
복제(clone)와 당겨오기(pull) 실습을 완료했습니다.
```

```
git add .
```
> ① 담기 ② 없음 ③ 카드 13

```
git commit -m "Docs: 복제 저장소에서 실습 한 줄 추가"
```
> ① 커밋 ⑰ (복제 폴더에서 생성) ② `[main ...]` ③ 카드 14

```
git push
```
> ① 복제 폴더의 커밋을 깃허브로 올림 ② 업로드 요약 ③ 카드 15

이제 **원래 폴더**로 돌아가 당겨옵니다.

```
cd ~/Desktop/python-quiz-game
```
> ① 원래 작업 폴더로 복귀 ② 출력 없음 ③ 카드 3

```
git pull
```
> ① 깃허브에 올라간 새 커밋(⑰)을 당겨옴 ② `Fast-forward`와 `README.md | 1 +` 요약 ③ `CONFLICT`면 멈추고 문의 (카드 18)

```
cat README.md
```
> ① 실습 한 줄이 원래 폴더에도 들어왔는지 확인 ② 맨 아래에 방금 문장이 보임 ③ 안 보이면 pull 결과 화면 복사 후 문의 (카드 5)

**복제 폴더 정리에 대해:** 지우지 않아도 아무 문제가 없으므로 **그대로 두는 것을 권합니다.**
정리가 꼭 필요하면, 먼저 복제 폴더에서 `git status`로 깨끗함을 확인하고 푸시가 끝났는지 확인한 뒤,
파인더(Finder)에서 폴더를 통째로 "보관용" 폴더로 **드래그해 옮기기만** 합니다. 삭제는 하지 않습니다.

---

## 13장. 16단계 — 제출물 정리 (선택 커밋 ⑱)

### 16-A. 스크린샷 찍기 (맥 캡처: 영역은 ⇧⌘4, 전체 화면은 ⇧⌘3, 기본 저장 위치는 바탕화면)

찍을 목록 (미션 제출 요건 기준):

1. `env.png` — 개발 환경: VS Code로 프로젝트가 열린 화면 + 터미널에 `python3 --version`과
   `git config user.name` 결과가 함께 보이게.
2. `menu.png` — 메뉴 화면.
3. `play.png` — 퀴즈 풀기(힌트 사용 장면 포함 권장) 결과 화면.
4. `add_quiz.png` — 퀴즈 추가 장면.
5. `list.png` — 퀴즈 목록.
6. `score.png` — 점수 확인(최고 점수 + 기록 히스토리).
7. `git_graph.png` — 아래 명령의 결과 화면.

```
git log --oneline --graph
```
> ① 제출용 커밋 그래프 표시 ② 병합에서 선이 갈라졌다 합쳐지는 모양 + 커밋 17개 이상 ③ 멈춘 듯하면 `q` (카드 20)

### 16-B. 스크린샷을 저장소에 넣기

```
git status
```
> ① 옮기기 전에 저장소가 깨끗한지(전부 커밋됐는지) 먼저 확인 ② `working tree clean` ③ 변경이 남았으면 커밋부터 (카드 8)

```
mkdir docs
```
> ① 문서용 폴더 생성 ② 출력 없음 ③ `File exists`면 그대로 사용 (카드 4)

```
mkdir docs/screenshots
```
> ① 캡처 보관 폴더 생성 ② 출력 없음 ③ 카드 4

이제 파인더에서 바탕화면의 캡처 파일들을 `python-quiz-game/docs/screenshots` 폴더로
**드래그해서 이동**하고, 파일 이름을 위 목록(`env.png` 등)으로 바꿉니다.
(이동·이름 변경 전에 위에서 `git status`로 깨끗함을 확인한 이유: 어떤 변화가 이번 작업에서
생긴 것인지 명확히 구분하기 위해서입니다.)

```
git add .
```
> ① 담기 ② 없음 ③ 카드 13

```
git commit -m "Docs: 실행 스크린샷 추가"
```
> ① 커밋 ⑱(선택) ② `[main ...]` ③ 카드 14

```
git push
```
> ① 올리기 ② 업로드 요약 ③ 카드 15

### 16-C. 제출 목록 최종 확인

- [ ] 깃허브 저장소 주소: `https://github.com/내아이디/python-quiz-game`
- [ ] 개발 환경 스크린샷 (`env.png`)
- [ ] 실행 결과 스크린샷 (메뉴·풀기·추가·목록·점수)
- [ ] `git log --oneline --graph` 스크린샷 (`git_graph.png`)
- [ ] README에 6개 필수 항목이 모두 있는지 (부록 C 그대로면 충족)

---

## 14장. 응용 변형 대비표 — 조건이 바뀌어도 원리로 대응하기

이 프로젝트의 핵심 원리는 하나입니다.

> **역할을 분리해 두면, 조건이 바뀔 때 고칠 곳이 한두 군데로 좁혀진다.**
> quiz.py = 데이터의 모양 / game.py = 게임의 흐름 / save_state·load_state = 저장 방식 /
> get_number_input·get_text_input = 입력 규칙 / 상수 = 자주 바뀌는 값.

시험이나 과제에서 나올 법한 변형 9가지와, 각각 **어디만 고치면 되는지**입니다.
변형 연습을 할 때는 네이토에게 "아래 변형을 적용해 파일 전체를 다시 출력하라"고 요청하면 됩니다.

| 변형 요구 | 고칠 위치 | 핵심 원리 |
|---|---|---|
| 1. 정답을 번호가 아니라 **글자(문자열)로 입력·비교** | quiz.py의 `answer` 속성 의미와 `check_answer`, game.py의 add_quiz 정답 입력(get_number_input → get_text_input), play_quiz 입력부 | 비교 규칙은 Quiz 안에 있으므로 게임 흐름은 거의 그대로 |
| 2. 선택지를 4개가 아니라 **N개(가변)로** | quiz.py `display`는 이미 반복문이라 무수정, add_quiz의 for 범위, play_quiz 입력 범위를 `len(quiz.choices)`로 | 4라는 숫자를 데이터 길이에서 얻으면 코드가 개수에 무감각해짐 |
| 3. 저장을 JSON이 아니라 **CSV로** | game.py의 `save_state`·`load_state` 두 메서드만 (Quiz의 to_dict/from_dict는 그대로 활용 가능) | 저장 방식이 두 메서드에 고립되어 있음 |
| 4. 데이터 파일 **이름·경로 변경** (예: data/quiz.json) | 상수 `DATA_FILE` 한 줄 (+폴더가 필요하면 저장 전 폴더 생성 한 줄) | 값을 상수로 빼 둔 이유 그 자체 |
| 5. **점수 규칙 변경** (예: 문제당 20점, 힌트 차감 10점) | play_quiz의 score 계산식 한 줄, 차감은 상수 `HINT_PENALTY` | 계산식이 한 곳에만 존재 |
| 6. **메뉴 항목 추가** (예: 7. 퀴즈 수정) | ⑴ 새 메서드 작성 ⑵ show_menu에 줄 추가 ⑶ run의 분기와 입력 범위 숫자 수정 — 항상 이 3곳 세트 | 메뉴 추가의 고정 레시피 |
| 7. **최고 점수를 점수가 아니라 정답 개수로** 관리 | play_quiz의 갱신 비교 대상과 show_score 표시, state.json의 best_score 의미 (README 스키마 설명도 수정) | "무엇을 기록하는가"의 정의는 한 곳에 모여 있음 |
| 8. **힌트를 문제당 1회만 허용** | play_quiz의 힌트 분기(문제별 사용 여부 변수 추가) | 상태(사용 여부)를 어느 범위에 두는가의 문제 |
| 9. 기록을 **파일 하나 더**(history.json)로 분리 | save_state·load_state에서 history만 따로 읽고 쓰게 분리 | 저장 로직 고립 덕에 파일 분할도 국소 수정 |

**응용의 최종 훈련(재현 훈련):** 시험 전날, 새 폴더에서 이 설계도의 2·3장(설계)만 보고
코드를 직접 다시 써 보세요. 막히는 메서드가 바로 본인의 약점입니다.

---

## 15장. 개념 → 코드 위치 지도 (금요일 시험 대비)

미션의 "과제 목표"에 있는 모든 개념이 이 프로젝트의 어디에 실물로 존재하는지의 지도입니다.
시험 공부 = 이 표의 오른쪽 칸을 열어 보며 왼쪽 개념을 설명해 보는 것.

| 설명할 수 있어야 하는 것 | 실물이 있는 곳 |
|---|---|
| 변수란, 왜 쓰나 | game.py의 `choice`, `count`, `hints_used` 등 모든 이름 붙은 값 |
| int / str / bool / list / dict 차이 | int: `answer`·점수 / str: `question` / bool: `check_answer` 반환 / list: `self.quizzes`, `choices` / dict: `to_dict` 반환값, state.json 구조 |
| if / elif / else | `run`의 메뉴 분기, 최고 점수 갱신 조건 |
| for와 while의 차이·선택 | for: 문제 출제 반복(횟수가 정해짐) / while: `get_number_input`(성공할 때까지 무한정) |
| 함수 정의·매개변수·반환값 | `get_number_input(prompt, min_num, max_num)` → 정수 반환 |
| 클래스란, 왜 쓰나 | quiz.py 맨 위 주석 + "데이터(Quiz)와 흐름(QuizGame)의 분리" |
| `__init__`과 self | quiz.py `__init__`의 속성 대입, 모든 메서드 첫 매개변수 |
| 속성과 메서드 | 속성: `self.question` 등 / 메서드: `display`, `check_answer` |
| 파일 열기·읽기·쓰기 | `save_state`·`load_state`의 `with open(...)` |
| JSON이란, 왜 저장에 쓰나 | `json.dump`·`json.load` + 2-3장 스키마 설명 |
| try/except 오류 처리 | 입력의 `ValueError`, 파일의 `FileNotFoundError`·`JSONDecodeError`, 종료의 `KeyboardInterrupt` |
| 깃이란, 왜 필요한가 | 4장 용어 사전 + 커밋 17개의 역사 자체 |
| init/add/commit/push/pull/checkout/clone | 사용한 곳: 1단계(init·add·commit·push) / 5단계(checkout, merge) / 15단계(clone·pull). 각 뜻은 6장 카드 |
| 브랜치 생성·병합 | 5단계 전체, `git log --oneline --graph`의 갈래 무늬 |

**리버싱(역설계) 학습 루틴 — 네이토에게 보내는 3종 세트 (파일은 9-3 방법으로 나눠 붙여넣기):**

```
아래 파일을 한 줄씩, 초보자 눈높이로 해설하라. 문법 이름을 말할 때마다 한국어 풀이를 함께 적어라.
```

```
아래 파일에서 중요한 부분 5곳을 빈칸으로 뚫은 연습 문제를 만들고, 내가 답하면 채점하라.
```

```
아래 파일을 보지 않고 처음부터 다시 만들려면 어떤 순서로 무엇을 떠올려야 하는지,
생각의 순서를 10줄 이내 목록으로 정리하라.
```

---

## 16장. 미션 요구사항 전수 점검표 (제출 전 마지막 훑기)

| 미션 요구 | 이 설계도에서 충족되는 곳 |
|---|---|
| 메뉴 출력·선택·종료·잘못된 입력 처리 | 2단계 |
| 공백 제거 / 변환 실패 / 범위 밖 / 빈 입력 재입력 | `get_number_input` (2단계) |
| Ctrl+C·입력 종료 시 저장 후 안전 종료 | 2단계 골격 + 9단계 저장 연결 |
| 파일 없음 → 기본 데이터 / 손상 → 안내 후 복구 | 9단계 `load_state` |
| Quiz 클래스 (question·choices·answer, 출력·정답 확인) | 3단계 |
| 기본 퀴즈 5개 이상 (주제 선택) | 4단계 (주제 교체 방법 포함) |
| 퀴즈 풀기: 출제·입력·정오 표시·결과·퀴즈 없음 처리 | 5단계 |
| 브랜치 생성 → 기능 커밋 → main 병합 | 5단계 (feature/quiz-play) |
| 퀴즈 추가: 입력·검증·파일 저장 | 6단계 + 9단계 연결 |
| 퀴즈 목록·없음 처리 | 7단계 |
| 최고 점수 확인·갱신·저장·기록 없음 처리 | 8단계 + 9단계 연결 |
| QuizGame 클래스 (목록·점수 속성, 기능 메서드) | 2~13단계 누적 |
| state.json 루트 저장, UTF-8 | 9단계 (`ensure_ascii=False`, `encoding="utf-8"`) |
| 보너스 1 랜덤 출제 | 10단계 |
| 보너스 2 문제 수 선택 | 10단계 |
| 보너스 3 힌트(속성·보기·차감) | 3단계 속성 + 11단계 |
| 보너스 4 퀴즈 삭제(파일 반영) | 12단계 |
| 보너스 5 기록 히스토리(날짜·문제 수·점수) | 13단계 |
| README 6항목 | 부록 C |
| 커밋 10개 이상 + 의미 있는 메시지(Feat/Fix/Docs/Refactor) | 1장 커밋 지도 (17~18개) |
| 깃 명령 7종 각 1회 이상 | init·add·commit·push(1단계), checkout(5), clone·pull(15) |
| clone → 수정 → push → pull 반영 확인 실습 | 15단계 |
| 파이썬 3.10 이상, 외부 라이브러리 금지 | 0단계 확인 + 표준 라이브러리(json·random·datetime)만 사용 |
| 함수 분리, 클래스 2개 이상 | 파일 3개 구조 (2장) |
| 제출물 4종 | 16단계 체크리스트 |

---

## 부록 A. main.py 전문 (참조용 — 네이토 결과와 비교 가능)

2단계에서 네이토가 만드는 main.py는 아래와 뜻이 같아야 합니다. (주석 문구는 달라도 됩니다.)

```python
# main.py
# ─────────────────────────────────────────────
# 역할: 프로그램의 시작점(입구) 파일.
# 실행 방법: 터미널에서  python3 main.py
# 실제 게임 내용은 전부 game.py의 QuizGame 클래스에 있고,
# 이 파일은 "만들고, 시작시키는" 두 가지만 한다.
# ─────────────────────────────────────────────

from game import QuizGame  # game.py 파일에서 QuizGame 클래스를 가져온다.

if __name__ == "__main__":
    # 이 파일을 "직접 실행"했을 때만 아래가 동작한다.
    # (다른 파일이 import 할 때는 실행되지 않게 막아 주는 관용구)
    game = QuizGame()   # 게임 객체(인스턴스)를 만든다. 이 순간 __init__이 실행된다.
    game.run()          # 게임의 큰 반복(메뉴 → 선택 → 기능)을 시작한다.
```

---

## 부록 B. quiz.py 전문 (참조용 정답 — 3단계 검수에 사용)

네이토 결과가 이상할 때 이 코드와 비교하거나, 아예 이 코드를 그대로 써도 됩니다.

```python
# quiz.py
# ─────────────────────────────────────────────
# 역할: "퀴즈 한 문제"를 표현하는 Quiz 클래스를 담는 파일.
# 게임 전체 흐름(game.py)과 분리해 두면,
# 퀴즈의 모양(속성·규칙)이 바뀔 때 이 파일만 고치면 된다.
# ─────────────────────────────────────────────


class Quiz:
    """퀴즈 한 문제(문제, 선택지 4개, 정답 번호, 힌트)를 담는 클래스."""

    def __init__(self, question, choices, answer, hint=None):
        # __init__: 객체가 만들어질 때 자동으로 한 번 실행되는 함수(생성자).
        # self: 지금 만들어지고 있는 "객체 자기 자신"을 가리키는 이름.
        # hint=None: 기본값. 힌트 없이 만들어도 되게 허용한다.
        self.question = question  # 문제 내용 (문자열, str)
        self.choices = choices    # 선택지 목록 (문자열 4개짜리 리스트, list)
        self.answer = answer      # 정답 번호 (정수, int) : 1~4
        self.hint = hint          # 힌트 (문자열 또는 None)

    def display(self, number=None):
        # 문제와 선택지를 화면에 출력한다. number는 "[문제 3]"처럼 붙일 순번.
        if number is not None:
            print(f"[문제 {number}]")  # f-문자열: 중괄호 안 값을 글자에 끼워 넣는다.
        print(self.question)
        print()
        # enumerate(리스트, start=1): 항목을 1번부터 번호와 함께 꺼내 준다.
        for i, choice in enumerate(self.choices, start=1):
            print(f"{i}. {choice}")
        print()

    def check_answer(self, user_answer):
        # 사용자가 고른 번호가 정답인지 True/False(불리언, bool)로 알려준다.
        return user_answer == self.answer

    def to_dict(self):
        # 이 객체를 딕셔너리(dict)로 바꾼다. → JSON 파일로 저장할 때 사용.
        return {
            "question": self.question,
            "choices": self.choices,
            "answer": self.answer,
            "hint": self.hint,
        }

    @classmethod
    def from_dict(cls, data):
        # 딕셔너리(JSON에서 읽은 것)를 다시 Quiz 객체로 되돌린다.
        # @classmethod: 객체가 아니라 "클래스 이름"으로 부르는 메서드. cls는 클래스 자신.
        # data.get("hint"): "hint" 열쇠가 없어도 오류 대신 None을 준다(옛 데이터와 호환).
        return cls(
            data["question"],
            data["choices"],
            data["answer"],
            data.get("hint"),
        )


if __name__ == "__main__":
    # 이 파일을 직접 실행(python3 quiz.py)했을 때만 도는 간단 자체 시험.
    sample = Quiz(
        "파이썬의 창시자는?",
        ["리누스 토르발스", "귀도 반 로섬", "제임스 고슬링", "브렌던 아이크"],
        2,
        "네덜란드 출신입니다.",
    )
    sample.display(1)
    print("2를 골랐을 때 →", sample.check_answer(2))   # True 가 나와야 정상
    print("딕셔너리 변환 →", sample.to_dict())
```

---

## 부록 C. README.md 전문 (14-B에서 통째로 붙여넣기, `[빈칸]` 2곳 채우기)

아래 네 겹 따옴표 상자 안의 내용 전체를 복사합니다.

````markdown
# 🎯 나만의 퀴즈 게임 (터미널 퀴즈 게임)

## 1. 프로젝트 개요

파이썬(Python) 기본 문법과 깃(Git)을 익히기 위해 만든 터미널 퀴즈 게임입니다.
메뉴에서 번호를 골라 퀴즈 풀기·추가·목록·삭제·점수 확인을 할 수 있고,
프로그램을 껐다 켜도 퀴즈와 점수가 `state.json` 파일에 남아 유지됩니다(데이터 영속성).
코드는 "퀴즈 한 문제"를 표현하는 `Quiz` 클래스와, 게임 전체 흐름을 관리하는
`QuizGame` 클래스로 역할을 나눠 구성했습니다.

## 2. 퀴즈 주제와 선정 이유

- 주제: 파이썬과 깃 기초 상식
- 선정 이유: [빈칸 — 본인의 말로 2~3문장. 참고 예시: 게임을 플레이하는 것만으로
  매주 보는 시험 범위(파이썬·깃 기초)를 자연스럽게 복습할 수 있도록,
  학습 내용 자체를 퀴즈 주제로 골랐습니다.]

## 3. 실행 방법

1. 파이썬 3.10 이상이 필요합니다. 확인: `python3 --version`
2. 터미널에서 이 프로젝트 폴더로 이동합니다.
3. 다음 명령으로 실행합니다.

```
python3 main.py
```

- 별도로 설치할 외부 라이브러리는 없습니다. (표준 라이브러리 `json`, `random`, `datetime`만 사용)

## 4. 기능 목록

| 메뉴 | 기능 | 설명 |
|---|---|---|
| 1 | 퀴즈 풀기 | 풀 문제 수를 고르면(보너스) 무작위로 문제를 뽑아(보너스) 출제. 각 문제에서 `0`을 입력하면 힌트를 보여주며, 힌트 1회당 5점 차감(보너스) |
| 2 | 퀴즈 추가 | 문제·선택지 4개·정답 번호·힌트(선택)를 입력해 등록하고 파일에 저장 |
| 3 | 퀴즈 목록 | 등록된 문제 목록 확인 (정답은 표시하지 않음) |
| 4 | 퀴즈 삭제 | 번호를 고르고 `y/n` 확인을 거쳐 삭제, 파일에 반영 (보너스) |
| 5 | 점수 확인 | 최고 점수와 최근 게임 기록(날짜·문제 수·점수, 보너스) 표시 |
| 6 | 종료 | 저장 후 안전 종료. `Ctrl + C`를 눌러도 저장 후 안전 종료 |

**입력 처리 규칙(공통):** 입력 앞뒤 공백 제거, 빈 입력·숫자가 아닌 입력·범위 밖 숫자는
안내 문구를 보여주고 다시 입력받습니다.

**점수 규칙:** 점수 = (맞힌 수 ÷ 푼 문제 수 × 100, 반올림) − (힌트 사용 횟수 × 5), 최저 0점.

## 5. 파일 구조

```
python-quiz-game/
├── main.py            # 시작점: QuizGame을 만들고 실행만 한다
├── quiz.py            # Quiz 클래스: 문제 1개의 속성(문제·선택지·정답·힌트)과 동작
├── game.py            # QuizGame 클래스: 메뉴·풀기·추가·목록·삭제·점수·저장/불러오기
├── state.json         # 데이터 파일 (프로그램이 자동 생성·관리)
├── README.md          # 이 문서
├── .gitignore         # 깃 제외 목록 (파이썬 임시 파일, 맥 시스템 파일 등)
└── docs/screenshots/  # 제출용 실행 화면 캡처
```

## 6. 데이터 파일 설명 (state.json)

- **경로:** 프로젝트 루트의 `state.json`
- **역할:** 퀴즈 목록, 최고 점수, 게임 기록을 저장해 프로그램 재시작 후에도 유지
- **인코딩:** UTF-8 (`ensure_ascii=False`로 저장해 한글이 그대로 보임)
- **안전 장치:** 파일이 없으면 기본 퀴즈 5개로 시작하고, 손상되어 있으면 안내 후
  기본 퀴즈로 복구합니다. `Ctrl + C`로 중단해도 저장 후 종료합니다.

**필드(스키마) 구조:**

| 열쇠 | 자료형 | 뜻 |
|---|---|---|
| `quizzes` | 리스트 | 퀴즈 목록. 각 항목은 `question`(문자열) / `choices`(문자열 4개 리스트) / `answer`(정수 1~4) / `hint`(문자열 또는 null) |
| `best_score` | 정수 또는 null | 최고 점수. 아직 안 풀었으면 null |
| `history` | 리스트 | 게임 기록. 각 항목은 `date`(문자열) / `total`(정수) / `correct`(정수) / `score`(정수) |

예시:

```json
{
  "quizzes": [
    {
      "question": "파이썬의 창시자는?",
      "choices": ["리누스 토르발스", "귀도 반 로섬", "제임스 고슬링", "브렌던 아이크"],
      "answer": 2,
      "hint": "네덜란드 출신입니다."
    }
  ],
  "best_score": 95,
  "history": [
    { "date": "2026-07-30 14:20", "total": 3, "correct": 3, "score": 95 }
  ]
}
```

> 참고: 실무에서는 데이터 파일을 `.gitignore`에 넣어 깃 추적에서 빼는 경우도 많지만,
> 이 과제에서는 시연과 채점 확인이 쉽도록 저장소에 포함했습니다.

## 7. 실행 화면

`docs/screenshots/` 폴더에 메뉴, 퀴즈 풀기, 퀴즈 추가, 목록, 점수 화면과
`git log --oneline --graph` 결과를 담았습니다.

## 8. 깃 작업 기록 요약

- 기능 단위로 커밋했으며 메시지는 `Feat:` `Docs:` `Refactor:` `Merge:` 형식을 사용했습니다.
- 퀴즈 풀기 기능은 `feature/quiz-play` 브랜치에서 개발한 뒤 main에 병합했습니다.
- 저장소를 별도 폴더에 `clone`해 수정·`push`하고, 원본 폴더에서 `pull`로 반영을 확인했습니다.
- 저장소 주소: https://github.com/[빈칸-깃허브아이디]/python-quiz-game
````

---

## 부록 D. game.py 설계 지도 (메서드 명세 — 응용·복습·검수의 기준표)

| 이름 | 역할 | 입력 | 반환 | 등장 단계 |
|---|---|---|---|---|
| (상수) `DATA_FILE` `HINT_PENALTY` `RECENT_HISTORY_COUNT` | 자주 바뀌는 값 모음 | — | — | 14 |
| `__init__(self)` | quizzes·best_score·history 초기화 후 `load_state()` 호출 | — | — | 2 → 9에서 완성 |
| `run(self)` | 메뉴 반복·분기, 종료·중단 시 저장 | — | — | 2 → 9에서 완성 |
| `show_menu(self)` | 메뉴 6항목 출력 | — | — | 2 |
| `get_number_input(self, prompt, min_num, max_num)` | 숫자 입력 검증(공백·빈값·문자·범위) | 안내문, 최소, 최대 | 정수 | 2 |
| `get_text_input(self, prompt)` | 빈 입력을 거르는 글자 입력 | 안내문 | 문자열 | 6 |
| `get_default_quizzes(self)` | 기본 퀴즈 5개 생성 | — | Quiz 리스트 | 4 |
| `play_quiz(self)` | 문제 수 선택→무작위 출제→힌트→채점→기록·저장 | — | — | 5 → 10·11·13에서 확장 |
| `add_quiz(self)` | 퀴즈 입력받아 등록·저장 | — | — | 6 → 9·11에서 확장 |
| `show_quiz_list(self)` | 문제 목록 표시(정답 미노출) | — | — | 7 |
| `delete_quiz(self)` | 목록→번호→y/n 확인→삭제·저장 | — | — | 12 |
| `show_score(self)` | 최고 점수·최근 기록 표시 | — | — | 8 → 13에서 확장 |
| `save_state(self)` | 상태를 state.json에 UTF-8로 저장 | — | — | 9 → 13에서 확장 |
| `load_state(self)` | 파일 읽기, 없음/손상 시 기본값 복구 | — | — | 9 → 13에서 확장 |

가져오는 표준 라이브러리: `json`(9), `random`(10), `datetime`(13) + `from quiz import Quiz`(4).

---

## 부록 E. 참고 자료와 근거 표시

이 문서의 근거는 두 종류로 나뉩니다. **정직성 원칙에 따라 구분해 둡니다.**

**⑴ 외부 문서로 확인한 절차 (2026-07-30 기준 검색으로 확인):**
- 깃허브 새 저장소 만들기 절차(오른쪽 위 + → New repository → 이름·공개 설정 → Create repository):
  깃허브 공식 문서 — https://docs.github.com/ko/repositories/creating-and-managing-repositories/creating-a-new-repository
  및 빠른 시작 — https://docs.github.com/ko/get-started/quickstart/create-a-repo
- 개인 접근 토큰(classic) 발급 절차(프로필 → Settings → Developer settings → Personal access tokens →
  Tokens (classic) → Generate new token, 생성 화면을 벗어나면 재확인 불가, 비밀번호 대신 사용):
  깃허브 공식 문서 — https://docs.github.com/ko/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token
- 2021년 8월부터 깃허브가 명령줄 비밀번호 인증을 없애고 토큰을 요구한다는 사실:
  실제 오류 문구를 다룬 정리 글 — https://soheelog.com/entry/Github-Token-%EB%B0%9C%EA%B8%89%ED%95%98%EA%B8%B0

**⑵ 안정된 표준 지식 (버전에 따라 변하지 않는 문법·명령):**
- 파이썬 문법과 표준 라이브러리: 공식 문서 — https://docs.python.org/ko/3/ (특히 json, random, datetime 항목)
- 깃 명령 상세: 공식 문서 — https://git-scm.com/docs

**⑶ 이 문서가 스스로 정한 설계 (근거가 "결정"인 부분):**
- 점수 계산식, 메뉴 구성, 메시지 문구, 파일 3개 구조, 커밋 메시지 문안 등은 미션 요구를 만족하는 범위에서
  이 설계도가 정한 값입니다. 각 결정의 이유와 반대 근거는 3장에 있습니다. 바꿔도 미션 위반이 아닌 항목입니다.

---

*(문서 끝. 막히는 화면이 나오면 단계 번호와 함께 화면을 그대로 복사해서 물어보세요.)*