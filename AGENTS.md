# 코디세이 E2 퀴즈 게임 프로젝트 작업 규칙

## 최우선 지시

모든 작업을 시작하기 전에 프로젝트 루트의 `E2_MASTER_PLAN.md`를 처음부터 끝까지 읽는다.

`E2_MASTER_PLAN.md`는 이 프로젝트의 최종 명세이자 작업 순서다.
문서의 기능, 파일 구조, 메서드 이름, 메시지 문구, 보너스 과제,
단계 구분, 검수 방법, Git 커밋 순서를 임의로 축소하거나 생략하지 않는다.

## 반드시 구현할 범위

- 필수 기능 전체
- 보너스 과제 5종 전체
- Python 클래스 2개 이상
- main.py, quiz.py, game.py 분리
- state.json UTF-8 저장
- 풍부한 한국어 주석
- 18개 이상의 의미 있는 커밋
- 브랜치 생성과 --no-ff 병합
- clone과 pull 실습
- README와 제출용 스크린샷 구조

## 파일 작성 규칙

각 단계에서 `E2_MASTER_PLAN.md`가 수정을 요구한 파일은
일부분만 임시로 패치하지 말고, 해당 단계의 완성 상태를 반영한
파일 전체 내용을 처음부터 끝까지 다시 작성하여 덮어쓴다.

코드에는 다음 주석을 풍부하게 작성한다.

- 파일 맨 위의 역할 설명
- 클래스의 역할
- 각 메서드의 역할
- 매개변수와 반환값
- self, __init__, enumerate, f-string 등 초보자가 혼동할 문법
- 조건문과 반복문을 선택한 이유
- JSON 변환과 예외 처리 이유
- 점수 계산과 상태 변경 이유

생략 기호, 의사 코드, TODO, “이전과 동일” 표현을 사용하지 않는다.

## 단계 진행 규칙

한 번에 한 단계만 수행한다.

각 단계에서 다음 순서를 지킨다.

1. 현재 Git 브랜치와 작업 상태 확인
2. 해당 단계의 P 지시문과 검수 항목 확인
3. 필요한 파일 전체 덮어쓰기
4. Python 문법 검사
5. 해당 단계의 실행 테스트
6. 변경 내용과 테스트 결과 보고
7. Git 커밋 직전에 멈춤

사용자가 커밋을 완료하고 “다음 단계 진행”이라고 입력하기 전에는
다음 단계로 넘어가지 않는다.

## Git 안전 규칙

- git add, commit, push, checkout, merge, clone, pull은
  사용자가 직접 실행할 수 있도록 정확한 명령을 제시한다.
- 사용자의 명시적 지시 없이 commit, push, merge를 실행하지 않는다.
- 파일 삭제, git reset --hard, force push를 실행하지 않는다.
- 각 단계 종료 시 권장 커밋 메시지를 정확히 제시한다.

## 검증 규칙

Python 파일 수정 후 다음 명령으로 문법을 검사한다.

`python3 -m py_compile main.py quiz.py game.py`

아직 존재하지 않는 파일은 검사 대상에서 제외한다.

실행 가능한 단계에서는 다음 명령을 사용한다.

`python3 main.py`

오류가 발생하면 다음 순서로 처리한다.

1. 오류가 발생한 파일과 줄
2. 오류 원인
3. 기대한 동작과 실제 동작
4. 수정 방법
5. 수정된 파일 전체 덮어쓰기
6. 같은 테스트 재실행

## 학습 규칙

각 단계가 끝나면 다음을 보고한다.

- 이번 단계에서 만든 기능
- 변경한 파일
- 새로 사용한 Python 문법 3개
- 코드 흐름
- 동료평가에서 설명해야 할 질문 3개
